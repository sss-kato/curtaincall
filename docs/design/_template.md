---
id: D-00
title: <設計書名。「〜設計」で終わる>
scope: app | collector | shared    # shared = 両方が従う共通仕様（JSON スキーマ・ID 規則など）
features: []            # カバーする機能 ID（例: [F-01, F-02]）。基盤設計は空
risks: []               # 対応するリスク ID（例: [R-2, R-8]）
depends_on: []          # 先に承認されているべき設計書（例: [D-01]）
status: draft           # draft | reviewed | approved
review_rounds: 0        # design-review が更新する
---

## 1. 目的と範囲

<この設計書が決めること・決めないこと。1段落>

## 2. 要件との対応

| 要件 | 本書での扱い | 節 |
|---|---|---|
| F-xx <機能名> | <どう実現するか一行> | §5.1 |
| R-xx <リスク名> | <どう緩和するか一行> | §6.2 |

<`features` / `risks` に挙げた ID はすべてこの表に載せる。載せられない ID は frontmatter から外す>

## 3. 構成

<層・モジュール・依存の方向。図（テキストまたは mermaid）と、ファイル配置を CLAUDE.md のディレクトリ規約に沿って示す>

```
src/
  domain/...
```

## 4. データ

<型定義・スキーマ・DB テーブル・設定ファイルの形式。要件 §7 と同じ名前・型を使う。コード片で示す>

## 5. 処理フロー（正常系）

### 5.1 <フロー名>

<入力 → 各ステップ → 出力。UseCase 単位で書く。1 UseCase = 1 責務>

## 6. 異常系・境界

| ケース | 期待する振る舞い | 担当する層 |
|---|---|---|
| <ネットワーク失敗、空データ、不正な形式、重複、上限超過 など> | | |

## 7. テスト方針

<UseCase ごとのテストケース一覧（正常系・異常系）と、必要なフィクスチャ・モック。CLAUDE.md「テスト方針」に従う>

| UseCase | ケース | 差し替えるもの |
|---|---|---|

## 8. 決定事項と根拠

| # | 決定 | 採らなかった案 | 理由 |
|---|---|---|---|

## 9. 未決定事項

<開発者の判断が必要な項目。design-doc が AskUserQuestion で確認し、回答後にこの節から §8 へ移す。承認時は空であること>

| # | 項目 | 選択肢 | 推奨 | 影響範囲 |
|---|---|---|---|---|

## 10. タスク分割の目安

<`/pm plan` が参照する。1 dev-loop で完了する単位（新規ファイル 5〜10、UseCase 1〜3）に分け、依存順に並べる>

| 順 | タスク候補 | 触るディレクトリ | 依存 |
|---|---|---|---|

## 変更履歴

| 日付 | 内容 |
|---|---|
| YYYY-MM-DD | 初版 |
