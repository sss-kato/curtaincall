---
id: T-01
title: collector スキャフォールド
stack: collector
features: []
depends_on: []
status: review
branch: task/T-01
worktree: ../CurtainCall-T-01
pr:
---

## 内容

D-02 §10 **S collector スキャフォールド** を実装する。

- 成果物: `collector/package.json`・`package-lock.json`・`tsconfig.json`・`eslint.config.js`・`.prettierrc.json`・`vitest.config.ts`・`.gitignore` 追記・`.github/workflows/collector-ci.yml`（§4.9・§5.6）。計 8
- 設計書側の依存: —

## 完了条件

- [x] `npm ci && npm run lint && npx tsc --noEmit && npx vitest run` が通る（テスト 0 件でも成功する設定にする）
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある（Task S の対象テストは 0 件）
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 S   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §4.9・§5.6
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`collector/`、`.github/workflows/`

## dev-loop 結果

## dev-loop 完了

タスク: T-01 collector スキャフォールド
対象: collector
周回: 第1段階 3 周 / 第2段階 3 周（打ち切り後、開発者判断で残 5 件を修正して検証へ直行）
検証: PASS（npm ci / lint / tsc / vitest（0 件）/ build）

### 変更ファイル
- collector/package.json・package-lock.json・tsconfig.json・tsconfig.build.json・eslint.config.js・.prettierrc.json・.prettierignore・vitest.config.ts・.node-version・.npmrc・src/main.ts（プレースホルダ）・test/.gitkeep
- .github/workflows/collector-ci.yml
- .gitignore（D-02 §4.9 の 4 行 + `.env`・サービスアカウント JSON のパターン）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 10 | `.gitignore` の秘密情報パターン、`.prettierignore`、CI に `npm run build`、`passWithNoTests` の include 拡張、`no-restricted-imports` の層ルール、`.node-version`・`engine-strict` |
| 1-2 | 7 | 層ルールの取りこぼし（裸の `crypto`、application の Node/外部ライブラリ、sources → `../http/`）、`fetch` グローバル、message 付き |
| 1-3 | — | 全員 PASS |
| 2-1 | 9 | 命名・コメント位置・`layerImportRule` 共通化・`infrastructureSiblings` 定数・tsconfig コメント・`globalThis.fetch`・`node:http` 等 |
| 2-2 | 6 | bare specifier を regex で全禁止（zod のみ許可）に一本化、`layerAllowedLibraries` |
| 2-3 | 5 | コメント文言・改名・regex エスケープ |

### 残っている「低」の指摘
- `actions/checkout@v4` 等のタグ参照（設計どおり。`collect.yml` では SHA 固定を検討）
- `eslint-config-prettier` に型宣言が無く `// @ts-check` の恩恵が default export で途切れる
- `.nvmrc` 不在（nvm は `.node-version` を読まない。手順で `nvm use 22` を明示）
- `npm audit` 13 件（devDependencies / firebase-admin の未使用経路。到達性評価済み）

### 対応しなかった指摘
- `../*/**` への置換（maintainability 2-2）：実測で `../../domain/x.js` を誤検知するため手書き列挙を維持（理由はコメントに記載）

### 開発者に確認してほしいこと
- **D-02 §4.9 との乖離 4 点（設計書側の追随が必要）**：(1) `build` は `tsc -p tsconfig.build.json`（`rootDir: src` と `include: test` が両立しないため分割）、(2) `tsconfig.json` の `include` から `eslint.config.js` を除外し `allowDefaultProject` で扱う、(3) devDependencies に `@eslint/js`・`eslint-config-prettier` を追加、(4) `.node-version`・`.npmrc`・`.prettierignore` を成果物に追加。次回 D-02 reopen 時に §3.2・§4.9・§10 S を更新
- **D-02 §5.6**：`collect.yml`（T-09）の `setup-node` も `node-version-file: collector/.node-version` に揃える
- **T-02 の完了条件に追記**：`vitest.config.ts` の `passWithNoTests: true` を削除する（最初にテストを追加するタスク）
- **設計書に無い lint 層ルール**を実装側で追加した（domain / application は bare specifier を全禁止で `zod` のみ許可、sources は他 infrastructure・`fetch`・`node:http` 等を禁止）。D-02 §4.9 への追記候補
- `npm audit` の critical/high は設計書のバージョン範囲（`vitest ^2`・`firebase-admin ^13`）内に修正版が無い。範囲を上げるかは D-02 reopen 時に判断
- ローカル既定の node が v20。`nvm use 22` が必要（`engine-strict` で 20 では `npm ci` が失敗する）

次のステップ:
1. この worktree（`/Users/shunpei/orca/projects/CurtainCall-T-01`）で最終レビューと動作確認
2. 問題なければ `/pm pr T-01`

