---
id: T-02
title: 契約スキーマと契約データ
stack: collector
features: []
depends_on: [T-01]
status: done
branch: task/T-02
worktree: ../CurtainCall-T-02
pr: https://github.com/sss-kato/curtaincall/pull/3
---

## 内容

D-01 §10 **A 契約スキーマと契約データ** を実装する。

- 成果物: `collector/src/domain/article.ts`（§4.1・§4.2）、`collector/src/domain/company.ts`（§4.3）、`data/companies.json`（§4.3 の内容）、`app/assets/companies.json`（`data/companies.json` の同一コピー）、`data/articles.json`（`schemaVersion: 1`、`generatedAt` はファイル作成時の JST 時刻を §4.1 の書式で、`articles: []`）、`collector/test/fixtures/contract/articles.sample.json`（§4.2 のサンプル）、`collector/test/domain/contract.test.ts`（§7.1 の 4 検証対象のみ）。計 7
- 設計書側の依存: D-02 のスキャフォールドタスク

## 完了条件

- [x] `npm run lint && npx tsc --noEmit && npx vitest run` が通る（契約テストが実ファイル `data/companies.json`・`app/assets/companies.json` を読むため、両ファイルを同じタスクで作る）
- [x] D-01 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] `collector/vitest.config.ts` の `passWithNoTests: true` を削除する（T-01 からの申し送り。最初にテストを追加するタスクのため）
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-01.md §10 A   ← 起点
- docs/design/D-01.md §3.2（ファイル配置）/ §4.1〜§4.3・§7.3
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`collector/src/domain/`、`collector/test/domain/`、`collector/test/fixtures/contract/`、`data/`、`app/assets/`

## dev-loop 結果

## dev-loop 完了

タスク: T-02 契約スキーマと契約データ（D-01 §10 Task A）
対象: collector
周回: 第1段階 1 周 / 第2段階 2 周
検証: PASS（npm ci / lint / tsc / vitest 19 件 / build）

### 変更ファイル
- collector/src/domain/article.ts・company.ts（D-01 §4.1〜§4.3 のコード片。`buildArticlesFileWriteSchema` の戻り値型を明示）
- data/companies.json・app/assets/companies.json（バイト同一）・data/articles.json（初期ファイル）
- collector/test/fixtures/contract/articles.sample.json・collector/test/domain/contract.test.ts（§7.1 の 4 検証対象、19 ケース）
- collector/vitest.config.ts（`passWithNoTests` を削除）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | — | 5 人全員 PASS（低 10） |
| 2-1 | 8 | schemaVersion 変更時の脆さ（101 件ケースを定数で組み立て失敗理由を検証、100 件ちょうどを追加）、`Raw*` をスキーマ由来の `Mutable*` に、`at()`、`HASH_LENGTH` |
| 2-2 | 4 | コメント・テスト名・メッセージ依存の緩和 |

### 残っている「低」の指摘
- `HashSchema` の `{16}` が `HASH_LENGTH` と連動していない／戻り値型の `z.ZodTypeDef` は zod v4 で廃止（型エイリアス化）— いずれも D-01 のコード片どおりのため見送り
- `superRefine` の issue に `path` が無い（D-01 のコード片どおり）
- `JSON.parse(...) as MutableArticlesFile` は `any` からの単一アサーション（`as unknown as` は lint に抵触）
- `DateTimeSchema` は暦として不正な値を通す／`title` が空白のみでも通る（設計どおり。破棄は application の責務）

### 対応しなかった指摘
- superRefine の `path`（D-01 追随を伴うため）

### 開発者に確認してほしいこと
- **D-01 次回 reopen 時**：§4.3 コード片の `./article` → `./article.js`、`HashSchema` を `HASH_LENGTH` から生成、superRefine に `path`、`DateTimeSchema` の暦検証を改訂候補に
- **D-02 へ申し送り**：`test/helpers/paths.ts` にリポジトリ相対パスを集約（契約テストと storage テストで共有）／storage テストで `data/articles.json` を `ArticlesFileSchema` で検証する 1 ケース／`collect-articles` テストに「空白のみの見出しは正規化後に空で破棄」

次のステップ:
1. この worktree で最終レビューと動作確認
2. 問題なければ `/pm pr T-02`

