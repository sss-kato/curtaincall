---
id: T-02
title: 契約スキーマと契約データ
stack: collector
features: []
depends_on: [T-01]
status: in_progress
branch: task/T-02
worktree: ../CurtainCall-T-02
pr:
---

## 内容

D-01 §10 **A 契約スキーマと契約データ** を実装する。

- 成果物: `collector/src/domain/article.ts`（§4.1・§4.2）、`collector/src/domain/company.ts`（§4.3）、`data/companies.json`（§4.3 の内容）、`app/assets/companies.json`（`data/companies.json` の同一コピー）、`data/articles.json`（`schemaVersion: 1`、`generatedAt` はファイル作成時の JST 時刻を §4.1 の書式で、`articles: []`）、`collector/test/fixtures/contract/articles.sample.json`（§4.2 のサンプル）、`collector/test/domain/contract.test.ts`（§7.1 の 4 検証対象のみ）。計 7
- 設計書側の依存: D-02 のスキャフォールドタスク

## 完了条件

- [ ] `npm run lint && npx tsc --noEmit && npx vitest run` が通る（契約テストが実ファイル `data/companies.json`・`app/assets/companies.json` を読むため、両ファイルを同じタスクで作る）
- [ ] D-01 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [ ] `collector/vitest.config.ts` の `passWithNoTests: true` を削除する（T-01 からの申し送り。最初にテストを追加するタスクのため）
- [ ] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-01.md §10 A   ← 起点
- docs/design/D-01.md §3.2（ファイル配置）/ §4.1〜§4.3・§7.3
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`collector/src/domain/`、`collector/test/domain/`、`collector/test/fixtures/contract/`、`data/`、`app/assets/`

## dev-loop 結果

