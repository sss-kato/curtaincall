---
id: T-03
title: 純粋関数群
stack: collector
features: []
depends_on: [T-02]
status: review
branch: task/T-03
worktree: ../CurtainCall-T-03
pr: https://github.com/sss-kato/curtaincall/pull/4
---

## 内容

D-01 §10 **B 純粋関数群** を実装する。

- 成果物: `collector/src/domain/url.ts`（§5.1）、`hasher.ts`、`text.ts`（§5.2・§4.8）、`article-id.ts`（§5.1）、`content-hash.ts`（§5.2）、`article-order.ts`（§4.5）、`category.ts`（§4.4）、`category-keywords.ts`（§4.4。`COMMON_CATEGORY_KEYWORDS` は `CategoryKeywordTable` 型で全カテゴリ空配列。値は D-03 のタスクで埋める）、`notification.ts`（§4.8）、`collector/src/infrastructure/hash/sha256-hasher.ts`（`Hasher` 実装）。計 10
- 設計書側の依存: A（`article.ts` の `Category`・`HASH_LENGTH`・`Article` を import する）

## 完了条件

- [x] `npm run lint && npx tsc --noEmit && npx vitest run` が通る。このタスクはテストファイルを追加しない（純粋関数の振る舞いは D-02 の UseCase テストで §7.2 のケースとして検証する。§8 #21）。`vitest run` は A の契約テストが引き続き通ることの確認
- [x] D-01 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-01.md §10 B   ← 起点
- docs/design/D-01.md §3.2（ファイル配置）/ §4.4・§4.5・§4.8・§5.1・§5.2・§7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`collector/src/domain/`、`collector/src/infrastructure/hash/`

## dev-loop 結果

## dev-loop 完了

タスク: T-03 純粋関数群（D-01 §10 Task B）
対象: collector
周回: 第1段階 2 周 / 第2段階 2 周
検証: PASS（npm ci / lint / tsc / vitest 19 件（契約テスト継続）/ build）

### 変更ファイル
- collector/src/domain/：url.ts・hasher.ts・text.ts・article-id.ts・content-hash.ts・article-order.ts・category.ts・category-keywords.ts・notification.ts
- collector/src/infrastructure/hash/sha256-hasher.ts

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 9 | **クエリ再シリアライズの非決定性**（追跡パラメータ削除の有無で `%20`/`+` が揺れ同じ記事が別 id → 常に `u.search = u.searchParams.toString()`）、エラー名・メッセージ切り詰め、`ReadonlySet`、負数ガード、空白キーワード、通知本文の空白圧縮、readonly |
| 1-2 | — | 全員 PASS |
| 2-1 | 10 | 逸脱 3 か所に「D-01 §x からの意図的な逸脱：理由」コメント、`URL_MAX`・`ERROR_INPUT_PREVIEW` 定数化、`cause` の入力全文漏れ、JSDoc、TODO(T-10)、§ の参照元 |
| 2-2 | — | 全員 PASS |

### 残っている「低」の指摘
- article-id.ts・content-hash.ts・hasher.ts の先頭にも § 参照元の注記を揃える／notification.ts のコメント重複／text.ts のガードコメントが呼び出し側の事情を書きすぎ
- `.slice(0, HASH_LENGTH)` の 2 か所重複（`shortHashHex` 集約は提案止まり）

### 対応しなかった指摘
- 制御文字（`\p{Cc}`）・方向制御／ゼロ幅（`\p{Cf}`）の除去、`normalizeUrl` の userinfo 拒否 — D-01 §5.1・§5.2 に規定が無く設計判断を要するため

### 開発者に確認してほしいこと（設計書側の追随）
- **D-01 次回 reopen 時（コード片を実装に追随）**：§5.1 `normalizeUrl` で削除の有無にかかわらず `u.search = u.searchParams.toString()`（クエリは form-urlencoded に統一、順序保持）／§4.8 `buildNotificationText` で制御文字置換後に連続空白圧縮＋trim／§4.4・§5.3 「空文字・空白のみのキーワードは無視。前後空白は含めない」／改訂候補：`\p{Cc}\p{Cf}` 除去・userinfo 拒否・`DateTimeSchema` の暦検証
- **D-02 §7.1 に追加するケース（T-06・T-08 の完了条件に含める）**：行 1 `collect-articles` に「`?p=1&q=a%20b` と `?p=1&q=a%20b&utm_source=x` → 同じ id」「`?p=1&q=%E3%81%82` の異表記が同じ id」／行 4 に「`" "` は一致しない、`" 初日 "` は前後空白込みで照合」／行 11 `notify-new-articles` に「`"\n\nA\n\nB\n"` → body `"A B"`」
- **D-03 T-10 の完了条件**に D-01 §7.2 行 4 の 9 ケース（`resolveCategory` / `matchCategoryTags` / `matchCategoryKeywords`）を含める（それまでこの 3 関数はテスト無し）

次のステップ:
1. この worktree で最終レビューと動作確認
2. 問題なければ `/pm pr T-03`

