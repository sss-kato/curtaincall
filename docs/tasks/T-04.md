---
id: T-04
title: domain のポートと日時変換
stack: collector
features: []
depends_on: [T-03]
status: review
branch: task/T-04
worktree: ../CurtainCall-T-04
pr:
---

## 内容

D-02 §10 **D domain のポートと日時変換** を実装する。

- 成果物: `src/domain/source.ts`・`collected-article.ts`・`http-client.ts`・`article-store.ts`・`articles-publisher.ts`・`notification-gateway.ts`・`logger.ts`・`clock.ts`・`datetime.ts`（§4.1〜§4.7）、`src/infrastructure/logging/console-logger.ts`、`src/infrastructure/clock/system-clock.ts`。計 11
- 設計書側の依存: B

## 完了条件

- [x] 型エラー・lint ゼロ。テストは追加しない（ポートに振る舞いが無く、`datetime.ts` は `collect-articles` / D-03 のテストで検証する）。ファイル数 11 は目安を超えるが、9 つは実装を伴わないポートの型定義（各 10〜30 行）で、実装を持つのは `console-logger.ts`・`system-clock.ts`・`datetime.ts` と `logger.ts` の `formatError`（§4.7）だけなので 1 dev-loop に収まる。`formatError` のテストは単独では書かず、`collect-articles`（§7.1 の Source 失敗行）と `notify-new-articles`（§7.1 の全団体 reject 行）のテストで間接検証する
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 D   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §4.1〜§4.7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/domain/`、`src/infrastructure/logging/`、`src/infrastructure/clock/`

## dev-loop 結果

## dev-loop 完了

タスク: T-04 domain のポートと日時変換
対象: collector
周回: 第1段階 1 周 / 第2段階 3 周
検証: PASS（lint / tsc / vitest 19 tests / prettier）

### 変更ファイル
- collector/src/domain/source.ts / collected-article.ts / http-client.ts / article-store.ts / articles-publisher.ts / notification-gateway.ts / clock.ts（ポート）
- collector/src/domain/logger.ts（Logger ポート・`formatError`・`parseLogLevel`）
- collector/src/domain/datetime.ts（`toJstDateTime`）
- collector/src/infrastructure/logging/console-logger.ts
- collector/src/infrastructure/clock/system-clock.ts

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 0 | 5 人 PASS |
| 2-1 | 7 | § ヘッダ・エラー `name` の宣言統一・JSDoc・`pad` 切り出し・`LEVEL_ORDER` 廃止（`LOG_LEVELS.indexOf`）・`JST_OFFSET_SUFFIX`・逸脱コメントの定型化・`formatError` の循環防御 |
| 2-2 | 6 | `formatError` を 1 引数に戻し再帰を `formatErrorChain` に分離・`ConsoleLogger` の検証を `parseLogLevel` に委譲・`parseLogLevel(string | undefined)`・引数名 `raw`・JSDoc・逸脱コメント導入句の統一 |
| 2-3 | 0 | 2 人 PASS |

### 残っている「低」の指摘
- `formatErrorChain` の JSDoc に `seen` を `ReadonlySet` で受けてコピーする理由を一言添える（対応は任意）
- 逸脱で追加した境界挙動（循環 cause・深さ上限 5・年範囲・引用規則）は間接テストでは通らない。D-02 追随時に §7 へ純粋関数のテーブル駆動テスト対象として列挙し、後続タスクで `test/domain/logger.test.ts`・`test/domain/datetime.test.ts` を追加する

### 対応しなかった指摘
- なし

### D-02 追随事項（コードの「意図的な追加」コメント 5 箇所と対応。D-02 反映時にコメントを一括削除する）
1. D-02 §4.2 `toJstDateTime`（datetime.ts）: 年が 1..9999 の範囲外なら `InvalidDateTimeError`。書式の 4 桁に収まらない年を後段の zod で分かりにくく落とさないため
2. D-02 §4.7 `formatError`（logger.ts）: cause の循環参照・深さ上限 5・`String()` 不能な値でも例外を投げず 1 行に収める。公開シグネチャは 1 引数のまま、再帰は非公開 `formatErrorChain`
3. D-02 §4.7 `needsQuoting`（console-logger.ts）: `/[\s=]/` に加えて引用符・制御文字・空文字もクォートする（ログの行崩れ・インジェクション対策）
4. D-02 §4.9 `parseLogLevel`（logger.ts）: `CURTAINCALL_LOG_LEVEL` を検証する関数を domain に追加。`string | undefined` を受け、未知・未設定は `undefined`
5. D-02 §4.9 `ConsoleLogger` コンストラクタ（console-logger.ts）: 未知レベルは `info` に縮退（型を信用しない防御）

### 後続タスクへの申し送り
- T-09（main.ts）: ログレベルは `parseLogLevel(process.env.CURTAINCALL_LOG_LEVEL) ?? "info"` の 1 段で書く
- T-06 / T-08 のテストで `formatError` を間接検証する（D-02 §7.1 の Source 失敗行・全団体 reject 行）

### 開発者に確認してほしいこと
- なし

