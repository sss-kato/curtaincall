---
id: T-05
title: http ラッパー
stack: collector
features: []
depends_on: [T-04]
status: review
branch: task/T-05
worktree: ../CurtainCall-T-05
pr:
---

## 内容

D-02 §10 **E http ラッパー** を実装する。

- 成果物: `src/infrastructure/http/fetch-http-client.ts`・`host-scheduler.ts`（§5.7）、`test/helpers/stub-fetch.ts`・`recording-logger.ts`、`test/infrastructure/http/fetch-http-client.test.ts`（§7.3 の 3 行）。計 5
- 設計書側の依存: D

## 完了条件

- [x] 品質ゲート通過。§7.3 の `FetchHttpClient` 3 行（リトライ／文字コード／ホスト間隔と上限）をすべて含む。実ネットワークへアクセスしない
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 E   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §4.3・§5.7・§7.3
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/http/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-05 http ラッパー
対象: collector
周回: 第1段階 3 周 / 第2段階 1 周（低のみ修正）
検証: PASS（lint / tsc / vitest 46 tests（http 27）/ prettier）

### 変更ファイル
- collector/src/infrastructure/http/fetch-http-client.ts（`FetchHttpClient`・`DEFAULT_FETCH_HTTP_CLIENT_OPTIONS`・ローカル `NonRetryableHttpError`）
- collector/src/infrastructure/http/host-scheduler.ts、sleep.ts（新設。D-02 §3.2 に無い）
- collector/test/helpers/stub-fetch.ts（出来事: response / timeout / abort / network-error / body-error、`finalUrl`・`delayMs`・signal 記録）、recording-logger.ts
- collector/test/infrastructure/http/fetch-http-client.test.ts（§7.3 の 3 group + リダイレクト group）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 14 | **高: 本文読み取り中のタイムアウトが HttpError にならずリトライもされない**（`arrayBuffer` を try 内へ）、AbortSignal 配線・予算消費のテスト、不正 URL、予算超過の cause、TimeoutError/AbortError、charset 異常値、body.cancel、message を設計書リテラルに、checkRedirect（https→http 拒否・ホスト変更 warn）、sleep 共通化 |
| 1-2 | 8 | **中: insecure redirect がリトライ対象**（`NonRetryableHttpError` で除外）、body.cancel を `rejectionFor` に集約、定型コメント、finalUrl 200 字、テストの message/cause/signal 検証、stub の aborted 早期 reject・型ガード・finalUrl オプション |
| 1-3 | 0 | 5 人 PASS |
| 2-1 | 9（低） | `isRetryable` 早期 return 分割、テスト名、doc 文言、`D-02 追随:` 接頭辞統一、`settled()`、`DEFAULT_FETCH_HTTP_CLIENT_OPTIONS` で既定値を 1 箇所に、重複テスト統合、real timers 前提コメント |

### 残っている「低」の指摘
- body.cancel が両経路で呼ばれることをテストで観測していない（stub に `bodyUsed` 観測を足せば可）
- `NonRetryableHttpError` の `name` が `HttpError` のまま（domain の `name` がリテラル型 `"HttpError"` のため override 不可。ログでは message で判別）
- `LOG_URL_PREVIEW` が domain の `ERROR_INPUT_PREVIEW` と同値の複製（D-01 追随待ち）
- checkRedirect の失敗はリトライしないが、`redirect: "follow"` のため拒否前に平文リクエスト 1 回は送られる（設計どおり）

### 対応しなかった指摘
- レスポンスサイズ上限（security 低）: D-02 §6 で意図的に不採用
- `TextDecoder` の `fatal`: 方針「取れる限り取る」で確定。ポート文言の修正は domain 追随（下記）

### D-02 追随事項（コード内 `D-02 追随:` コメントと対応）
1. §4.3 `getText` doc: 「リダイレクトは追う」の例外（https→http 降格は HttpError で拒否・非リトライ）、不正 URL は送信せず HttpError・予算を消費しない、「デコード失敗は HttpError」→「未知ラベルは utf-8 にフォールバック（warn）」
2. §5.7: 手順 2 と 3 の間に「リダイレクト検査」（(a) https→http 拒否＝infrastructure 内の HttpError サブクラスで非リトライ、(b) ホスト変更は warn（finalUrl 200 字）、(c) `response.url` 空・パース不能はスキップ）。手順 3 に「非 2xx・リダイレクト拒否とも `body.cancel()` してから投げる」。手順 4 の `AbortError` → Node 22 は `TimeoutError`。手順 0 に「リトライで予算超過したときは直前の失敗を cause に」。charset ラベルは `[a-zA-Z0-9_-]+` に限定。`DEFAULT_FETCH_HTTP_CLIENT_OPTIONS` の export
3. §3.2: `infrastructure/http/sleep.ts` を追加
4. §6: 「https→http リダイレクト → HttpError、リトライせず Source 失敗」「ホスト変更リダイレクト → 追従して warn。転送先ホストは HostScheduler の間隔・予算のホスト単位で扱われない」「不正 URL → 送信せず HttpError、予算を消費しない」
5. §7.3: リダイレクト系・不正 URL・実時間タイムアウト（`cause.name === "TimeoutError"`・`signal.aborted`）・予算超過の cause の各ケース。§7.2 に `stubFetch()` の出来事（timeout / abort / body-error / finalUrl / delayMs）
6. §8.1: D-03 宛「Source は別ホストへのリダイレクトを前提にしない（`Company.sources` は最終 URL）。https→http に降格する情報源は http URL を直接指定するか除外」。D-01 宛「`ERROR_INPUT_PREVIEW` の export または `text.ts` にログ用切り詰め関数」。§8 決定事項に「https→http 降格の拒否は §4.3 の意図的な例外」

### 後続タスクへの申し送り
- T-09（main.ts）: `new FetchHttpClient({ ...DEFAULT_FETCH_HTTP_CLIENT_OPTIONS, userAgent, ...環境変数の上書き }, logger)`
- T-10〜T-15（Source）: テストは `StubHttpClient`（D-03 §7.1）を使い `stubFetch` は使わない。`RecordingLogger` は共通

### 開発者に確認してほしいこと
- なし

