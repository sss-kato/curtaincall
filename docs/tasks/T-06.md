---
id: T-06
title: collect-articles と detect-diff
stack: collector
features: [F-11]
depends_on: [T-04, T-05]
status: review
branch: task/T-06
worktree: ../CurtainCall-T-06
pr:
---

## 内容

D-02 §10 **F collect-articles と detect-diff** を実装する。

- 成果物: `src/application/collect-articles.ts`・`detect-diff.ts`（§5.1・§5.2）、`test/helpers/`（`StubSource`・`FixedClock`・`buildCompanies`。`RecordingLogger` は E で作る）、`test/application/collect-articles.test.ts`・`detect-diff.test.ts`。計 7
- 設計書側の依存: D、B、E（`RecordingLogger`）

## 完了条件

- [x] 品質ゲート通過。§7.1 の `collect-articles` 10 行・`detect-diff` 5 行をすべて含む
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 F   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §5.1・§5.2・§7
- 画面定義書: なし（collector）
- docs/requirements.md §4.1 F-11
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/application/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-06 collect-articles と detect-diff
対象: collector
周回: 第1段階 3 周 / 第2段階 2 周
検証: PASS（lint / tsc / vitest 113 tests（collect 51・detect-diff 16）/ prettier）

### 変更ファイル
- collector/src/application/collect-articles.ts（`CollectArticles`。`settleTarget` / `adoptArticles` / `buildCollectedArticle` に分割、`RawArticleShapeSchema`（zod、domain 移設候補）、`previewRawValue`）
- collector/src/application/detect-diff.ts（`DetectDiff`。`resolveArticle` / `countKinds` / `hasSameArticles`（`ARTICLE_KEYS` は `ArticleSchema` から導出））
- collector/test/helpers/stub-source.ts（`rejectWith: unknown`・`throwSyncOnFetch`・`delayMs`・`receivedOptions`）、fixed-clock.ts、build-companies.ts、build-company.ts、array.ts
- collector/test/application/collect-articles.test.ts（§7.1 の 10 行 + 追加 8 件）、detect-diff.test.ts（§7.1 の 5 行 + 追加 1 件）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 12 | **中: 型契約に反する RawArticle で execute 全体が reject**、§7.1 行 4「category」欠落、行 5・9 の `it` 連結、生値ログの 200 字上限、非 Error reject・同期 throw・ちょうど 100 件のテスト、JSDoc |
| 1-2 | 6 | **高: 手順 6 の try/catch が hasher 例外を飲み込む** → try/catch 削除、`RawArticleShapeSchema` で形を値として弾く、hasher 例外は reject（テスト追加） |
| 1-3 | 0 | 5 人 PASS |
| 2-1 | 13 | **中: `hasSameArticles` のキー列挙を型で束縛**、`execute` 150 行の分割、`buildCollectedArticle` を private メソッドに、`resolveArticle` 切り出し、3 シナリオ 1 `it` を 4 `it` に、命名・コメント、テストヘルパ共通化、`buildCompanies()` 使用 |
| 2-2 | 5（低） | `ARTICLE_KEYS` を zod から導出（eslint-disable 撤去）、`at()` 分離、前提アサート、`CollectionState`、戻り値型の命名 |

### 残っている「低」の指摘
- `RawArticleShapeSchema` は `domain/source.ts` へ移設し `RawArticle` を `z.infer` から導出する（D-02 追随後）
- `invalid raw article` ログは `path:code` だけに絞る案
- `companyId mismatch` の `previewRawValue` は形検査後は不要

### 対応しなかった指摘
- なし

### D-02 追随事項（コード内 `D-02 追随:` コメントと対応）
1. §5.1 手順 3〜4 の間: `fetch()` の解決値が配列でなければ `reason: "error"`, `message: "fetch() did not return an array"`, `warn("source returned non-array")`
2. §5.1 手順 5〜6 の間: `RawArticleShapeSchema.safeParse` で形を検査し、失敗は `warn("invalid raw article")` で記事単位に破棄・`discardedByCompany` に加算。手順 6 内の例外（hasher 等）は捕捉せず `execute()` を reject（§8 に決定理由）。スキーマの置き場は `domain/source.ts`（§3.2）
3. §5.1 ログ規約: Source 由来の生値は `previewRawValue`（200 UTF-16 単位、`String()` 不能は `[unprintable]`）を通す。§4.6 `discardedByCompany` の破棄理由に「形の不正」を追加
4. §5.2 手順 9: 「キーの有無と値の両方」→「10 フィールドを値で比較（キー無しと `undefined` は JSON 上同一のため区別しない）」
5. §7.1 行 7「Source 失敗」に 非 Error reject／同期 throw／形の契約違反（title undefined・thumbnail null・要素 null・非配列）／hasher 例外は reject、行 12 に「ちょうど 100 件」
6. §7.2: `bindingOf(company, options)`、`buildCompanies()` の利用タスク、`test/helpers/build-company.ts`・`array.ts`。行 4 category の「D-03 へ移管」の読み違え（本行の 2 項目は collect-articles が含める）
7. D-01 追随: `previewRawValue` を `domain/text.ts` の `previewForLog` に寄せる候補

### 後続タスクへの申し送り
- T-07 / T-09: `test/helpers/build-company.ts`（`company(id)`）・`array.ts`（`at`）・`stub-source.ts`（`bindingOf`・`unimplementedBindingOf`）を再利用。T-08 のテスト内ローカル `buildCompany` / `buildArticle` は T-09 で `build-company.ts` に寄せる
- T-09: `CollectArticles(bindings, hasher, logger)`・`DetectDiff(logger)` のコンストラクタ

### 開発者に確認してほしいこと
- なし

