---
id: T-07
title: storage と publish-articles
stack: collector
features: []
depends_on: [T-04, T-05]
status: review
branch: task/T-07
worktree: ../CurtainCall-T-07
pr:
---

## 内容

D-02 §10 **G storage と publish-articles** を実装する。

- 成果物: `src/infrastructure/storage/articles-file-store.ts`・`companies-file-store.ts`・`git-articles-publisher.ts`・`json-format.ts`・`run-summary-store.ts`・`noop-article-writer.ts`・`noop-articles-publisher.ts`、`src/application/publish-articles.ts`、`test/helpers/`（`InMemoryArticleStore`・`FakePublisher`）、`test/application/publish-articles.test.ts`、`test/infrastructure/storage/articles-file-store.test.ts`（§7.3 の 2 行）。計 12
- 設計書側の依存: D、E（`RecordingLogger`）

## 完了条件

- [x] 品質ゲート通過。§7.1 の `publish-articles` 1 行と §7.3 の `ArticlesFileStore` 2 行を含む
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 G   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §5.3・§5.4・§7.3
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/storage/`、`src/application/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-07 storage と publish-articles
対象: collector
周回: 第1段階 3 周 / 第2段階 2 周
検証: PASS（lint / tsc / vitest 157 tests / prettier / tsconfig.build）

### 変更ファイル
- collector/src/infrastructure/storage/articles-file-store.ts（原子的書き出し `.tmp` → `writeFile({flush})` → `rename`。ENOENT は info・他は warn）、companies-file-store.ts、git-articles-publisher.ts（`execFile` 引数配列・`GitCommandError`・`timeout` 60 s・`GIT_TERMINAL_PROMPT=0`・失敗時は `origin/main` からパス限定で復元）、json-format.ts（`formatJson`）、run-summary-store.ts（`Readonly<Record<string, unknown>>`）、noop-article-writer.ts、noop-articles-publisher.ts、sanitize-git-output.ts（新設。認証情報のマスク・500 字）、paths.ts（新設。`ARTICLES_JSON_RELATIVE_PATH`・`PUBLISH_BRANCH`）
- collector/src/application/publish-articles.ts（`PublishArticles`・`ArticlesValidationError`）
- collector/test/helpers/in-memory-article-store.ts・fake-publisher.ts、test/application/publish-articles.test.ts（7）、test/infrastructure/storage/articles-file-store.test.ts（11）・sanitize-git-output.test.ts（10）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 10 | 101 件テストの issues 検証、自前 `ExecFileError` → `ExecFileException`、`RunSummaryStore.write` の型、`formatJson` 共通化、`as` 撤去、ENOENT/他の分岐、部分書き込み後の unlink 検証、stderr マスク |
| 1-2 | 7 | **中: stderr マスクが `error.message` と `cause` で迂回される** → `git()` ラッパーで `GitCommandError`（無害化済み message、cause なし）に変換、マスクを純粋関数に切り出しテーブル駆動テスト |
| 1-3 | 0 | 5 人 PASS |
| 2-1 | 10 | 手順 3-n 統一、レビュー ID 削除、ENOENT→info の理由コメント、`GitCommandError` の name/code/signal・narrowing、`timeout`・`GIT_TERMINAL_PROMPT`、`sanitizeGitOutput` 改名、`paths.ts` |
| 2-2 | 0 | 2 人 PASS |

### 残っている「低」の指摘
- `sanitizeGitOutput` の引数名・`STDERR_LOG_LIMIT` が stderr 限定の名前のまま
- 復元失敗 warn の `data/articles.json` リテラルを `ARTICLES_JSON_RELATIVE_PATH` に
- `GitCommandError.signal` フィールドは message 以外で未参照

### 対応しなかった指摘
- なし

### D-02 追随事項（コード内 `D-02 追随:` コメントと対応）
1. §3.2 `storage/` に `sanitize-git-output.ts`・`paths.ts` を追加、§10 G のファイル数を更新
2. §5.3 手順 3: `git()` ラッパーが execFile の失敗を `GitCommandError`（無害化済み message・`code`・cause なし）で再スロー。手順 3-5 は「1〜4 のいずれか」（add の失敗も含む）。手順 3-5・3-6 のログは `{ error: formatError(error) }` のみ（`stderr` フィールド無し）。`execFile` に `timeout` 60 s・`GIT_TERMINAL_PROMPT=0`
3. §4 `ArticleReader` / §6 / §7.3: `readPrevious` のファイル無し（ENOENT）は `info`、ENOENT 以外の読み取り失敗・不正 JSON・スキーマ不一致は `warn`
4. §4.8・§3.2: `RunSummaryStore.write` は `Readonly<Record<string, unknown>>` を受け `RunSummary` に依存しない（§3.1 の依存方向）
5. §7.1 / §7.3: 追加ケース（issues 6 件以上で先頭 5 件、rename 中の例外、部分書き込み後の失敗、EACCES）と `sanitizeGitOutput` / `maskCredentials` のテーブル駆動テスト行。§8 に「秘密情報の無害化は純粋関数として直接テストする」
6. §4.6: `ArticlesValidationError` の `name` はフィールド宣言。§5.3 `ArticlesFileStore.write`: `flush: true`、失敗時 `.tmp` を unlink、ディレクトリ fsync は行わない

### 後続タスクへの申し送り
- T-09: `RunSummary.sourceFailures[].message` は `$GITHUB_STEP_SUMMARY` に Markdown として流れるため固定書式・改行除去・200 字上限を run-collection 側で。`FakePublisher.calls`（共有配列）と `invocationCallOrder` の 2 方式を 1 つに統一。`article()` / `articlesFile()` ビルダを `test/helpers/build-article.ts` に寄せる（T-06 / T-08 の申し送りと同件）。`GitArticlesPublisher` をテストするなら `runGit` を注入点に。`RunSummaryStore.write` には `RunSummary` を渡す（型は `Readonly<Record<string, unknown>>`）

### 開発者に確認してほしいこと
- なし

