---
id: T-08
title: FCM と notify
stack: collector
features: [F-07]
depends_on: [T-04]
status: done
branch: task/T-08
worktree: ../CurtainCall-T-08
pr: https://github.com/sss-kato/curtaincall/pull/10
---

## 内容

D-02 §10 **H FCM と notify** を実装する。

- 成果物: `src/infrastructure/fcm/firebase-notification-gateway.ts`・`noop-notification-gateway.ts`、`src/application/notify-new-articles.ts`、`test/helpers/fake-notification-gateway.ts`、`test/application/notify-new-articles.test.ts`。計 5
- 設計書側の依存: D、B（`buildNotificationText`）

## 完了条件

- [x] 品質ゲート通過。§7.1 の `notify-new-articles` 4 行を含む
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 H   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §5.4・§7
- 画面定義書: なし（collector）
- docs/requirements.md §4.1 F-07
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/fcm/`、`src/application/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-08 FCM と notify
対象: collector
周回: 第1段階 3 周 / 第2段階 1 周（低のみ修正）
検証: PASS（lint / tsc / vitest 62 tests（notify 16）/ prettier）

### 変更ファイル
- collector/src/infrastructure/fcm/firebase-notification-gateway.ts（zod でサービスアカウント JSON を検証、固定文言 2 種、`cert` は常に検証・`initializeApp` は初回のみ）
- collector/src/infrastructure/fcm/noop-notification-gateway.ts
- collector/src/application/notify-new-articles.ts
- collector/test/helpers/fake-notification-gateway.ts（`rejectTopics`・`mode: "reject" | "throw"`）
- collector/test/application/notify-new-articles.test.ts（§7.1 の 4 行 15 項目 + 同期 throw）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 8 | **高: サービスアカウント parse/cert に型ガードが無く、`JSON.parse` の SyntaxError.message 経由で秘密鍵の断片がログに漏れる経路** → try/catch で固定文言（cause なし）、zod で camelCase/snake_case を safeParse。§7.1 4 行目「全団体が reject」追加、サロゲートペア境界テスト修正、message と cause の二重化解消、文面組み立てを try 内へ、Fake に同期 throw |
| 1-2 | 5 | §7.1 読み替え規則どおり `it` を分割、`parseServiceAccount` を常に実行、JSON 不正と鍵不正で文言分離、`ParsedServiceAccount` 型、`as string` 撤廃 |
| 1-3 | 0 | 5 人 PASS |
| 2-1 | 4（低） | レビュー ID をコメントから削除し `D-02 追随:` に統一、型宣言の位置、`cert` を `if` の外へ、テストビルダを async 化 |

### 残っている「低」の指摘
- send 失敗の `cause` が firebase-admin の例外をそのままログ連鎖に乗せる（現状の版では鍵断片を含まない。多層防御として `code` のみに包み直す案）
- FCM の受付 messageId を記録していない（Logger 注入は設計外）

### 対応しなかった指摘
- レスポンス（`parseServiceAccount`）の異常入力テスト: D-02 §7 冒頭で `FirebaseNotificationGateway` は単体テスト対象外

### D-02 追随事項
1. §5.4: サービスアカウント JSON は zod で検証（`projectId`/`clientEmail`/`privateKey` または snake_case）。失敗は入力の抜粋を含まない固定文言 2 種（JSON・スキーマ不正／鍵不正）で `Error`（cause なし）。`parseServiceAccount`・`cert` は `getApps()` の状態に関わらず常に実行。main.ts（T-09）が捕捉して終了コード 1（§5.5 手順 4・§6 の該当行も拡張）
2. §5.4: `NotificationSendError` の message は固定文言 `"failed to send notification"` のみ。原因は `cause`（`formatError` が連結する）
3. §7.1「送信の基本」: 「`send` が同期的に throw しても他団体は送られる」を追加。§7.2 `FakeNotificationGateway` に `mode: "throw"`

### 後続タスクへの申し送り
- T-09: `eslint.config.js` に `firebase-admin` を `infrastructure/fcm/` 以外から禁止する規則を追加。テスト内ローカルの `buildCompany` / `buildArticle` を `test/helpers/build-company.ts` / `build-article.ts` に昇格。`FakeNotificationGateway` に呼び出し順の共有配列が要れば後方互換のオプション引数で
- 別タスク候補: `firebase-admin@14` への更新（推移的依存 `uuid` の moderate 勧告。messaging からは到達しない）
- D-01 §4.8 追随: `CONTROL_CHARS` を `\p{Cc}`・双方向制御文字・ゼロ幅文字に拡張

### 開発者に確認してほしいこと
- なし

