---
id: T-09
title: run-collection・main.ts・収集ワークフロー
stack: collector
features: [F-07, F-11]
depends_on: [T-06, T-07, T-08]
status: review
branch: task/T-09
worktree: ../CurtainCall-T-09
pr:
---

## 内容

D-02 §10 **I run-collection・main.ts・収集ワークフロー** を実装する。

- 成果物: `src/application/full-crawl-policy.ts`・`run-collection.ts`（§4.8・§5.5）、`src/main.ts`（§5.5。`SOURCE_FACTORIES` は空で作り D-03 が埋める）、`.github/workflows/collect.yml`（§5.6）、`test/application/run-collection.test.ts`。計 5
- 設計書側の依存: F、G、H

## 完了条件

- [x] 品質ゲート通過。§7.1 の `run-collection` 3 行（順序の不変条件／安全条件／fullCrawl）を含む。`CURTAINCALL_DRY_RUN=1` のローカル実行が `SOURCE_FACTORIES` 空（全団体 `not_implemented`）で最後まで通り、終了コード 0・`failures` 5 件のサマリが書かれる
- [x] D-02 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §10 I   ← 起点
- docs/design/D-02.md §3.2（ファイル配置）/ §4.8・§5.5・§5.6・§7
- 画面定義書: なし（collector）
- docs/requirements.md §4.1 F-07 / F-11
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/application/`、`src/`、`.github/workflows/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-09 run-collection・main.ts・収集ワークフロー
対象: collector
周回: 第1段階 3 周 / 第2段階 3 周
検証: PASS（lint / tsc / prettier / vitest 378 tests。`CURTAINCALL_DRY_RUN=1 npm start` 終了コード 0、`failures` 5 件 `not_implemented`）

### 変更ファイル
- `collector/src/application/full-crawl-policy.ts`（新規。`decideFullCrawlCompanyIds` 純粋関数）
- `collector/src/application/run-collection.ts`（新規。`RunCollection.execute()` 手順 1〜10、`warnCompaniesWithoutArticles`・`notifyIfPublished` を抽出。`RunSummary = CompletedRunSummary | FailedRunSummary`（`kind`）、`buildFailureSummary({ clock, ... })`、`sanitizeFailures`・`sanitizeFailureMessage`（C0/C1/U+2028/2029 を空白、200 字）、`SnapshotMissingError`、`buildCommitMessage`）
- `collector/src/main.ts`（DI 組み立て。`SOURCE_FACTORIES` は空で T-16 が埋める。`SourceDeps`、`writeSummary`、`nonEmptyEnv`。`.run-summary.json` の書き出し失敗は warn + 終了コード 0、致命的失敗のサマリに `SnapshotMissingError.failures` を載せる）
- `.github/workflows/collect.yml`（新規。D-02 §5.6。Action は 40 桁 SHA 固定、`permissions: {}` + job 単位、jq の欠損ガード、`node-version-file`）、`collector-ci.yml`（Action の SHA 固定）、`.github/dependabot.yml`（新規。github-actions weekly）
- `collector/test/application/run-collection.test.ts`（新規。24 tests。§7.1 の 3 group + `buildFailureSummary` + `sanitizeFailureMessage` 境界）
- `collector/test/helpers/build-article.ts`（新規。`buildArticle`・`buildArticlesFile`・`buildCollectedArticle` の昇格）、`in-memory-article-store.ts`（`calls` 共有配列・`readPreviousCallCount`）、既存 3 テストの import 統一

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 7 | `sourceFailures[].message` の無害化（中）、Action の SHA 固定（中）、job 単位 `permissions`（中）、サマリ書き出し失敗を warn に（中）、致命的失敗に `failures` を載せる（中）、Noop に手順 1 の logger |
| 1-2 | 9 | `buildFailureSummary` を application へ・型を `RunSummary` に（中）、`sanitizeFailureMessage` の境界 5 ケース（中）、`buildSummary` の引数型、`nonEmptyEnv` 統一、テストの `instanceof`・値比較・例外ケース、jq の `unknown` フォールバック、JSDoc |
| 1-3 | 0 | 3 人 PASS（低 6 → 第 2 段階へ） |
| 2-1 | 11 | テスト名の矛盾を 2 本に分離（中）、`execute()` 95 行を抽出（中）、`RunSummary` の判別可能ユニオン、`Clock` を `buildFailureSummary` に、`writeSummary`、`buildCollectedArticle` 昇格、`StubArticleReader` 廃止、手順番号順 |
| 2-2 | 4 | U+2028/2029 を `\uXXXX` エスケープに（中）、レビュー ID 全削除、jq コメント、docstring |
| 2-3 | 0 | readability PASS |

### 残っている「低」の指摘
- `FailedRunSummary` の型 docstring と `buildFailureSummary` の関数 docstring の理由文が重複（片方に寄せる）

### 対応しなかった指摘
- なし

### D-02 追随事項（設計書を reopen するときにまとめて反映）
- §4.8: `RunSummary` を `CompletedRunSummary`（`kind: "completed"`、`durationMs: number`、`previousSnapshot: boolean`）と `FailedRunSummary`（`kind: "failed"`、両方 `null` 固定）の判別可能ユニオンに。`sourceFailures[].message` は「1 行・200 字（UTF-16 コード単位）。C0/C1 制御文字・U+2028/2029 は空白」
- §5.5: 失敗時サマリの組み立て（`buildFailureSummary`）は application（`RunCollection` と同じファイル）。`Clock` を受け取り `generatedAt` を内部で算出。main.ts は `writeSummary` で呼ぶだけ。手順 3 の warn 条件を「`previous` があり、`fullCrawlCompanyIds` が空で、その団体の記事が 0 件」に改訂（`articles: []` の初期状態で 5 件の不要な案内が出る。**実装変更は設計書改訂後**）
- §5.6: Action は 40 桁 SHA + バージョンコメントで固定（`dependabot.yml` で更新）、`permissions: {}` + job 単位（`collect: contents: write`、`deploy-pages: pages/id-token: write`）、`node-version-file: collector/.node-version`、jq は `.sourceFailures | if type == "array" then length else "unknown" end`・`(.x // [])` の欠損ガード、ステップサマリの message は `gsub`/`.[0:200]` で多層防御
- §7.1: `run-collection` に「`buildFailureSummary` が `SnapshotMissingError.failures` を無害化して載せる」「`sanitizeFailureMessage` の境界 6 ケース」「collectArticles が例外 → publish・notify は呼ばれず伝播」「diff の `newArticlesByCompany` を加工せず渡す」を追加。§7.2 に `build-article.ts`（`buildCollectedArticle`）

### 後続への申し送り
- T-16（DI 配線）: `SOURCE_FACTORIES` に 5 行（`createXxxSource` の import + 表）を埋めるだけで済む構造。`eslint.config.js` に `no-restricted-imports` で `firebase-admin` を `infrastructure/fcm/` 以外から禁止する規則を追加。`collector/README.md`（ローカル実行 `CURTAINCALL_DRY_RUN=1 npm start`、環境変数一覧、団体の追加手順）を完了条件に含める
- `upload-pages-artifact` は v3 系最新（v3.0.1）を維持。v4 への更新は別途

### 開発者に確認してほしいこと
- なし

