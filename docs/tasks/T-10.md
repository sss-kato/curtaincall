---
id: T-10
title: 共通キーワード表と HTTP スタブ
stack: collector
features: []
depends_on: [T-04]
status: review
branch: task/T-10
worktree: ../CurtainCall-T-10
pr: https://github.com/sss-kato/curtaincall/pull/13
---

## 内容

D-03 §10 **J 共通キーワード表と HTTP スタブ** を実装する。

- 成果物: `src/domain/category-keywords.ts`（変更。§4.6 の `COMMON_CATEGORY_KEYWORDS` の値）、`test/helpers/stub-http-client.ts`（新規。§7.1）。計 2
- 設計書側の依存: D-01 Task B、D-02 Task D

## 完了条件

- [x] 品質ゲート通過。D-01 の契約テストが引き続き通る。`COMMON_CATEGORY_KEYWORDS` の 7 キーがすべて存在し `other` が空配列
- [x] D-03 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-03.md §10 J   ← 起点
- docs/design/D-03.md §3.2（ファイル配置）/ §4.6・§7.1
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/domain/`、`test/helpers/`

## dev-loop 結果

## dev-loop 完了

タスク: T-10 共通キーワード表と HTTP スタブ
対象: collector
周回: 第1段階 2 周（第 2 周は adversarial / spec のみ。差分がテストのみのため）/ 第2段階 1 周
検証: PASS（lint / tsc / vitest 140 tests / prettier）

### 変更ファイル
- collector/src/domain/category-keywords.ts（D-03 §4.6 の 62 語を転記。`TODO(T-10)` 削除）
- collector/test/helpers/stub-http-client.ts（D-03 §7.1。`Object.hasOwn` で prototype キーを除外）
- collector/test/domain/category.test.ts（D-01 §7.2 行 4 の 9 ケース + 表側 fold + 実表の不変条件。12 tests）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 5 | 実表の不変条件テスト（7 キー・other 空・空白のみ無し・fold 後の重複無し）、表側の大文字・全角キーワードの fold、空白のみキーワード（`" "`・`"　"`・`"\t"`）、ケース 5 の対比、`Object.hasOwn` |
| 1-2 | 0 | 2 人 PASS |
| 2-1 | 0 | 2 人 PASS（低のみ） |

### 残っている「低」の指摘
- テストの空 `CategoryKeywordTable` 6 回を `tableWith()` ヘルパに、団体固有語 `"Z"` の命名、対比ケース末尾の重複アサーション
- 不変条件テストを 3 `it` に分け違反キーワードを出力する形に
- `trim()` で落ちない ZWSP・BOM のガード（D-01 §5.3 追随時に実装側と揃える）

### 対応しなかった指摘
- なし

### D-01 / D-03 追随事項
1. D-01 §7.2 行 4: 「キーワード照合が全角半角・大文字小文字を無視する（見出し側の fold）」「空文字・空白のみのキーワードは一致しない」に文言更新。「表側の fold」ケースを追加
2. D-01 §4.4・§5.3: 「空文字のキーワードは無視」→「空文字・空白のみ（`trim()` 後に空）のキーワードは無視」（T-03 の逸脱の追随）
3. D-03 §7: 共通表の不変条件テスト（7 キー・`other` 空・空白のみ無し・fold 後の重複無し）を列挙
4. D-03 §7.1: `StubHttpClient.getText` は `async` なし（`require-await`）。`Object.hasOwn` で自身のプロパティのみ参照

### 後続タスクへの申し送り
- T-11〜T-15（Source）: classify のテストで「タグ = キャスト、見出しにチケット → cast」（タグ段階確定時にキーワード段階を無視）を固定する。`StubHttpClient` は `new StubHttpClient({ [url]: html })` でフィクスチャを返す。フィクスチャは各テストファイルから `fs.readFileSync(new URL(..., import.meta.url))` で読む（D-03 §7.2）

### 開発者に確認してほしいこと
- なし

