---
id: T-12
title: 新感線 Source
stack: collector
features: []
depends_on: [T-10, T-05, T-06]
status: done
branch: task/T-12
worktree: ../CurtainCall-T-12
pr: https://github.com/sss-kato/curtaincall/pull/17
---

## 内容

D-03 §10 **K2 新感線 Source** を実装する。

- 成果物: `src/infrastructure/sources/shinkansen.ts`、`test/infrastructure/sources/shinkansen.test.ts`、`test/fixtures/sources/shinkansen/feed.xml`・`README.md`。計 4
- 設計書側の依存: J、E、F

## 完了条件

- [x] 品質ゲート通過。§7.3 の共通 3 行 + 新感線 2 行（部分一致「先行上映」→ `ticket` の固定を含む）。フィクスチャの取得 URL が `blog.vi-shinkansen.co.jp`（R-9）。robots.txt 再確認（制限があれば上記の扱い）
- [x] D-03 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-03.md §10 K2   ← 起点
- docs/design/D-03.md §3.2（ファイル配置）/ §4・§5.3・§7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/sources/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-12 新感線 Source
対象: collector
周回: 第1段階 1 周 / 第2段階 2 周
検証: PASS（lint / tsc / prettier / vitest 303 tests）

### 変更ファイル
- `collector/src/infrastructure/sources/shinkansen.ts`（新規。T-11 `horipro.ts` をコメントごと同文で写し、団体固有の値（`COMPANY_ID`・`SHINKANSEN_TABLES`・§5.3 参照・R-9 の doc コメント）だけ変えた。5 人のレビュアーが `diff` で差分が団体固有値のみであることを確認）
- `collector/test/infrastructure/sources/shinkansen.test.ts`（新規。68 tests。`horipro.test.ts` と同構造）
- `collector/test/fixtures/sources/shinkansen/{feed.xml,README.md}`（新規。`blog.vi-shinkansen.co.jp/?feed=rss2` の実応答 10 件。robots.txt 404。`media:*`/`enclosure` を持つ項目が無いためサムネイルは合成フィードで検証）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 0 | 5 人 PASS（低 2） |
| 2-1 | 4 | class の doc コメントに R-9 の理由（公式ドメインのフィードが空）、フィクスチャ describe の合成 RSS `it` をカテゴリ describe へ統合、`requests` の期待値を `FEED_URL` 定数に、「（ホリプロと同じ）」を削除 |
| 2-2 | 0 | readability PASS |

### 残っている「低」の指摘
- class の doc コメント「URL はコードに持たない」の主語を「取得先 URL はコードに持たず、companies.json の company.sources から受け取る」に（ホリプロ側と同時に揃える）

### 対応しなかった指摘
- なし

### D-03 追随事項（ホリプロ T-11 と同じ。設計書 reopen 時にまとめて反映）
- docs/tasks/T-11.md の「D-03 追随事項」10 項目をそのまま踏襲（TZ 無し pubDate は `invalid_date`、日付順 `pubDate → dc:date → isoDate`、型ガード、http(s) のみ、ログ url 200 字、`build-rss.ts`）
- §7.3 新感線行: 「部分一致の固定」はフィクスチャ describe ではなくカテゴリ describe（合成 RSS）に置く

### 後続への申し送り（ホリプロと同時に直すもの）
- テストの `(loggedUrl as string).length` を `typeof` ガードに
- 実装・テストの `200` を `LOG_URL_MAX_LENGTH` 定数に
- class の doc コメントの主語（上記「低」）

### `infrastructure/rss/` への共通化案（maintainability の提案。D-03 §8 #1 の改訂が要る。T-16 完了後に別タスクで検討）
- 現状: ホリプロ・新感線で約 200 行（型・型ガード・`createParser`・`extractThumbnail`・`resolvePublishedAt`・`asAbsoluteUrl`・`firstNonEmpty`・`extractOne` 本体）が完全同文。rss-parser のメジャー更新で 2 か所同時修正になる
- 切り出し単位（純粋関数の集合。基底クラスは採らない）: `infrastructure/rss/feed-item.ts`（`FeedItem`・`MediaField`・`isMediaField`・`isImageMedia`・`asText`・`asMediaFields`）、`infrastructure/rss/parser.ts`（`createParser`）、`infrastructure/rss/extract.ts`（`extractThumbnail`・`resolvePublishedAt`+`NAIVE_TIME_SUFFIX_RE`・`firstNonEmpty`・`extractRssArticle(item, { companyId, tables })`）。`asAbsoluteUrl` は HTML 3 団体でも要るので `domain/url.ts`（D-01 改訂）へ
- 各 `sources/<id>.ts` に残す: `COMPANY_ID`・`*_TABLES`・`SourceDeps`・`classify`（D-01 改訂まで）・class（`fetch()` と破棄集計）・factory。import は `sources/<id>.ts → infrastructure/rss/` の一方向のみ
- D-03 §8 #1 改訂案: 「Source 間の共通化は (a) domain の純粋関数、(b) `infrastructure/rss/`（RSS）・`infrastructure/html/`（HTML、必要になった時点で）の純粋関数を各 Source から呼ぶ形に限る。(b) に置けるのはパーサーライブラリの出力形に依存するため domain に置けない型・型ガード・parser 生成・`FeedItem → 値` の写像に限り、団体名・カテゴリ表・取得 URL を知ってはならない」。§7.3「共通アサーションのヘルパは作らない」は「`infrastructure/rss/` の関数は `test/infrastructure/rss/` で直接テストし、各 Source テストは契約・破棄・フィクスチャに絞る」に改める。**CLAUDE.md「infrastructure 間でコピーしない」との整合は開発者判断**（「パーサー出力形に依存するものに限り infrastructure 内の共通層を許す」の 1 行追記）
- D-01 改訂候補: `COMMON_CATEGORY_TAGS`（団体横断で同値のタグ写像。`チケット → ticket`・`配信 → streaming` 等）、`normalizeTitle` で HTML タグ・C0 制御文字を除去（security の提案）

### 開発者に確認してほしいこと
- なし（`infrastructure/rss/` 共通化と CLAUDE.md 追記は T-16 後の判断事項として上記に記録）

