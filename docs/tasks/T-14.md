---
id: T-14
title: 四季 Source
stack: collector
features: []
depends_on: [T-10, T-05, T-06]
status: review
branch: task/T-14
worktree: ../CurtainCall-T-14
pr:
---

## 内容

D-03 §10 **K4 四季 Source** を実装する。

- 成果物: `src/infrastructure/sources/shiki.ts`、`test/infrastructure/sources/shiki.test.ts`、`test/fixtures/sources/shiki/news-p1.html`・`news-p2.html`・`news-last.html`・`README.md`。計 6
- 設計書側の依存: J、E、F

## 完了条件

- [x] 品質ゲート通過。§7.3 の共通 3 行 + 四季 3 行（ページ送り 11 ケース：途中ページの `HttpError`・`visited` の正規化比較・2 ページ目以降の `matched` 0 件と全項目破棄が `warn` + 部分返却になること（§5.5、§8 #17）を含む／フィクスチャ（画像リンクの `no_title` を含む）／サムネイル）。`SELECTORS.item` を 1 本に確定し項目数 = 返却件数・`url` 重複なしを固定（§4.2）。`SELECTORS.next` を 1 要素の配列に確定し `:contains` を残さない。robots.txt 再確認（制限があれば上記の扱い）
- [x] D-03 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-03.md §10 K4   ← 起点
- docs/design/D-03.md §3.2（ファイル配置）/ §4・§5.5・§7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/sources/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-14 四季 Source
対象: collector
周回: 第1段階 2 周 / 第2段階 2 周
検証: PASS（lint / tsc / prettier / vitest 330 tests）

### 変更ファイル
- `collector/src/infrastructure/sources/shiki.ts`（新規。cheerio で `article.block` を走査。`SELECTORS`（`item: "article.block"`・`link: "a.table"`・`title: ".title"`・`dateText: "time.date"`・`next: [".pagination li.next a"]`・`excludeFromImage: ".tag"`。設計初期値は実 DOM に無く実測で確定）、`SHIKI_TABLES`、`SHIKI_FULL_CRAWL_PAGES = 5`。日付は `time[datetime]` → テキスト `2026.09.12` の 2 段。ページ送りは §5.5 の終端 (a)〜(d) に加え (e) 別オリジン・(f) URL 長超過を warn で終端。終端判定は `fetch` 1 か所に一元化し `parsePage` は集計専用（`{ articles, matched, discarded, nextHref }`）。サイト非依存部分は宝塚 `takarazuka.ts` と同文）
- `collector/test/infrastructure/sources/shiki.test.ts`（新規。46 tests。ページ送り 11 ケース＋別オリジン・URL 長超過・datetime 2 段の非対称）
- `collector/test/fixtures/sources/shiki/{news-p1,news-p2,news-last}.html`・`README.md`（新規。`news-last.html` は p3 から `li.next` を `<span>` 化した派生。robots.txt 制限なし）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 6 | 「全記事の url が正規表現に一致」を全件ループに（中）、別オリジン・URL 長超過の終端、datetime 2 段判定のテスト、`news-last.html` の派生形、3 ページ目を取りに行かないことの固定、終端判定を `fetch` に一元化 |
| 1-2 | 0 | 5 人 PASS（低 7 → 第 2 段階へ持ち越し） |
| 2-1 | 6 | URL 長超過の終端に warn（4 人が指摘）、warn の `next` を `truncateUtf16`、`origin` を 1 回計算、レビュー ID 削除・終端 (e)(f) のコメント対、テストの `SHIKI_FULL_CRAWL_PAGES`・`URL_MAX` 導出 |
| 2-2 | 0 | readability PASS |

### 残っている「低」の指摘
- `ParsedPage` の JSDoc「(a)〜(d)」を「(a)〜(f)」に
- 終端 warn のフィールドを `{ sourceId, url（現在ページ）, next, fullCrawl }` に統一（(c) は `url` に次ページを入れている）
- `fetch` が約 90 行（`resolveNextUrl` の切り出しは `fetch` が 60 行に収まらず見送り。将来の共通化時に再検討）

### 対応しなかった指摘
- なし

### D-03 追随事項（設計書を reopen するときにまとめて反映）
- §4.2 四季: `SELECTORS` を実 DOM に確定（`item: "article.block"`・`link: "a.table"`・`title: ".title"`・`dateText: "time.date"`・`next: [".pagination li.next a"]`・`excludeFromImage: ".tag"`）。設計候補の `a[rel="next"]`・`:contains` は実 DOM に無い
- §4.4 四季: `datetime` 属性が `DATE_ATTR_RE` に一致しないときだけテキストへ落ちる（一致するが暦日不正なら `invalid_date`）
- §5.5: 終端 (e) 次へリンクが 1 ページ目と別オリジン → `warn("next link points to another origin", { sourceId, url, next, fullCrawl })`、(f) `normalizeUrl(next)` が `InvalidArticleUrlError` → `warn("next link is not a valid article url", { ..., reason })`。いずれも部分返却で終端。§6 に四季行として同内容を追加。ホスト移転時は companies.json の URL を更新する
- §5.5 / §7.3: `noimage_1.png` プレースホルダーをサムネイルに採用するか（**開発者判断**。除外規定なし。除外するなら `extractThumbnail` で pathname 照合）
- §7.2 四季行: `news-last.html` は「2 ページ目または次のページ」から派生してよい（連結テストで URL が重複しないよう p3 から派生）
- §7.3 共通行「classify の等価性」: `siteTags` が常に空の四季・東宝はタグ段階が発動しないことを固定（期待値はキーワード段階の結果）
- §7.3 四季行: 「`a[rel="next"]` より前に『次へのご案内』」は `next` が 1 要素なので「pagination 外の無関係な『次へ』は辿らない」に読み替え
- §8 #1: 全項目破棄の判定位置は `fetch` 側（四季の形）を基準にし、宝塚 `takarazuka.ts` を共通化時に揃える

### 後続への申し送り
- T-15（東宝）: 同文で写したサイト非依存部分は `shiki.ts` と `takarazuka.ts` で `diff` 済み。乖離は全項目破棄の判定位置・`ExtractResult.isUrlFallback`・`extractThumbnail` の JSDoc・コンストラクタコメントのみ
- `listPage === undefined` 分岐は到達不能（宝塚同文）。共通化時に `listPage: URL` 必須へ

### 開発者に確認してほしいこと
- `noimage_1.png`（四季の「画像なし」プレースホルダー）を `thumbnail` に採用したままでよいか（app の記事セルに灰色画像が出る）

