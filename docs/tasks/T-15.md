---
id: T-15
title: 東宝 Source
stack: collector
features: []
depends_on: [T-10, T-05, T-06]
status: todo
branch:
worktree:
pr:
---

## 内容

D-03 §10 **K5 東宝 Source** を実装する。

- 成果物: `src/infrastructure/sources/toho.ts`、`test/infrastructure/sources/toho.test.ts`、`test/fixtures/sources/toho/topics.html`・`news.html`・`README.md`。計 5
- 設計書側の依存: J、E、F

## 完了条件

- [ ] 品質ゲート通過。§7.3 の共通 3 行 + 東宝 4 行（2 URL・片方 0 件の `SourceError`・topics と news をまたぐ同一記事が 2 件返ること／正規化 6 ケース（`stagegate.jp`・`example.com` は変更なし）／フィクスチャ（画像リンクの `no_title` を含む）／サムネイル）。`SELECTORS.item` を 1 本に確定し項目数 = 返却件数・同一ページ内の `url` 重複なしを固定（§4.2）。`/stage/news/` の一覧に恒常的に 1 件以上載るか（過去記事が年別ページへ退避して一覧が空になる構造でないか）をフィクスチャ取得時に確認し、§7.2 のとおり `README.md` に記録する。退避する構造なら、`sources[]` から `news` を外す案（`data/companies.json` の変更。D-01 §4.3 の改訂が要る）を PR 説明に書いて開発者の判断を仰ぐ（§8 #16 の固着回避）。topics と news でセレクタが異なればファイル内で pathname により分け、§7.2 のとおり `README.md` へ記録。フィクスチャに `stagegate.jp` へのリンクが現れたら `TOHO_HOSTS` への追加可否を PR 説明に書く（§4.3）。robots.txt 再確認（制限があれば上記の扱い）
- [ ] D-03 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [ ] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-03.md §10 K5   ← 起点
- docs/design/D-03.md §3.2（ファイル配置）/ §4・§5.6・§7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/sources/`、`test/`

## dev-loop 結果

