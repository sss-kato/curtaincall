---
id: T-15
title: 東宝 Source
stack: collector
features: []
depends_on: [T-10, T-05, T-06]
status: review
branch: task/T-15
worktree: ../CurtainCall-T-15
pr:
---

## 内容

D-03 §10 **K5 東宝 Source** を実装する。

- 成果物: `src/infrastructure/sources/toho.ts`、`test/infrastructure/sources/toho.test.ts`、`test/fixtures/sources/toho/topics.html`・`news.html`・`README.md`。計 5
- 設計書側の依存: J、E、F

## 完了条件

- [x] 品質ゲート通過。§7.3 の共通 3 行 + 東宝 4 行（2 URL・片方 0 件の `SourceError`・topics と news をまたぐ同一記事が 2 件返ること／正規化 6 ケース（`stagegate.jp`・`example.com` は変更なし）／フィクスチャ（画像リンクの `no_title` を含む）／サムネイル）。`SELECTORS.item` を 1 本に確定し項目数 = 返却件数・同一ページ内の `url` 重複なしを固定（§4.2）。`/stage/news/` の一覧に恒常的に 1 件以上載るか（過去記事が年別ページへ退避して一覧が空になる構造でないか）をフィクスチャ取得時に確認し、§7.2 のとおり `README.md` に記録する。退避する構造なら、`sources[]` から `news` を外す案（`data/companies.json` の変更。D-01 §4.3 の改訂が要る）を PR 説明に書いて開発者の判断を仰ぐ（§8 #16 の固着回避）。topics と news でセレクタが異なればファイル内で pathname により分け、§7.2 のとおり `README.md` へ記録。フィクスチャに `stagegate.jp` へのリンクが現れたら `TOHO_HOSTS` への追加可否を PR 説明に書く（§4.3）。robots.txt 再確認（制限があれば上記の扱い）
- [x] D-03 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-03.md §10 K5   ← 起点
- docs/design/D-03.md §3.2（ファイル配置）/ §4・§5.6・§7
- 画面定義書: なし（collector）
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（collector 節・テスト方針）

## 触るファイルの見込み

`src/infrastructure/sources/`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-15 東宝 Source
対象: collector
周回: 第1段階 3 周 / 第2段階 2 周
検証: PASS（lint / tsc / prettier / vitest 399 tests）

### 変更ファイル
- `collector/src/infrastructure/sources/toho.ts`（新規。`sources[]`（topics → news）を逐次取得。item セレクタは topics `.news-item-img` / news `.news-item` で異なり `selectItemSelector`（`endsWith("/stage/topics/")` 等。コンストラクタで fail-fast）で分岐。`normalizeTohoUrl`・`TOHO_HOSTS`・`TOHO_HOST_ALIASES` は D-03 §4.3 のとおり。日付は `time[datetime]` → `DATE_KANJI_RE` の 2 段。`siteTags` は実在する `.news-tag` から取得（`tagMap` は空）。片方 0 件は `SourceError("no items in")`、URL をまたぐ重複は除去しない。サイト非依存部分は宝塚・四季と同文）
- `collector/test/infrastructure/sources/toho.test.ts`（新規。47 tests。正規化 6 ケースは Source 経由）
- `collector/test/fixtures/sources/toho/{topics,news}.html`・`README.md`（新規。topics 12 件・news 4 件。robots.txt `Allow: /`。外部ドメイン・`/tamiou/` 重複・`stagegate.jp` の記録）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 8 | 正規化 6 ケースを Source 経由に（中）、`TOHO_TABLES` コメントの矛盾、`selectItemSelector` の末尾正規化と想定外パスの `SourceError`、`invalid_date`・見出しフォールバックの正の経路テスト、`parseDate` の `exec` 一本化、`normalizeTohoUrl` の JSDoc |
| 1-2 | 2 | 末尾スラッシュ無しの正の経路テスト（中）、想定外パスをコンストラクタで fail-fast |
| 1-3 | 0 | adversarial PASS（低 1） |
| 2-1 | 6 | `TOHO_HOSTS` JSDoc の経緯削除、`endsWith` 判定、重複を除去しない理由コメント、テストの手書き DOM を `topicsItem()` 経由に |
| 2-2 | 0 | readability PASS |

### 残っている「低」の指摘
- `TOHO_HOSTS` の JSDoc に経緯（D-03 との食い違い）が 1 行残る
- `selectItemSelector` の JSDoc 後半の表記（`/topics/`）を前半（`/stage/topics/`）に揃える

### 対応しなかった指摘
- なし

### D-03 追随事項（設計書を reopen するときにまとめて反映）
- §4.2 東宝: item セレクタは topics `.news-item-img`・news `.news-item`（pathname で分岐）。`SELECTORS.tags: ".news-tag"`、`excludeFromImage: ".news-tag-wrap"`
- §4.6・§5.6 東宝: 「サイト側タグ無し／`siteTags` 空配列」→「`.news-tag` の各テキスト（`上演決定`・`映像配信`・`新着情報`・`お知らせ`）。`tagMap` は空（汎用タグが多く、`上演決定`・`映像配信` は見出しのキーワードで拾える。タグ段階で確定すると見出しの「チケット」「配信」より優先されて潰す副作用がある）」
- §5.6: `selectItemSelector` は末尾スラッシュを正規化し、`/stage/topics/`・`/stage/news/` 以外はコンストラクタで `SourceError("unsupported page")`（HTTP 取得前に fail-fast）
- §7.3 共通行・§10 K5「同一ページ内の url 重複なし」→「多重一致の検知は件数一致で担保し、サイト運用による同一遷移先の重複（`/stage/topics/` の `/tamiou/` 1 組）は README 記録の既知分のみ許容」
- §7.3 東宝行: 正規化 6 ケースは `topicsItem({ href })` の合成 HTML で Source 経由。「`2026-13-45` → `invalid_date`（テキストにフォールバックしない）」「見出しフォールバックの正の経路」「末尾スラッシュ無し」「想定外パス → 構築時 `SourceError`」を追加
- §7.3 共通行「classify の等価性」: 東宝は `.news-tag` から `siteTags` を取るのでタグ優先ケースがそのまま成立する（四季は不成立）

### 開発者に確認してほしいこと
- `stagegate.jp` を `TOHO_HOSTS` に追加するか（`/stage/topics/` 先頭項目のリンク先が `https://stagegate.jp/` トップページ。個別記事 URL の形式が不明なため現状は正規化せずそのまま通す。D-03 §4.3）
- `/stage/news/` は 4 件（`data-max-page="1"`）で恒常的に載るため `sources[]` に残した（年別ページへの退避なし）

### 後続への申し送り（T-16 後の共通化タスクの入力）
3 ファイル（宝塚・四季・東宝）で完全同文: `SourceDeps`・`CategoryTables`・`classify`・`DiscardReason`・`toHttpUrl`・`listPageKey`・`pointsToListPage`・`resolveHref`・`ParsedYmd`・`toDateOnlyResult`・`ymdFromMatch`・`extractThumbnail`（本体）・`THUMBNAIL_ATTRS`・`DATE_ATTR_RE`（四季・東宝）・`parsePage` の破棄集計ループ・`extractArticle` の判定順とログ形式。乖離 9 点:
1. `ExtractResult`: 宝塚のみ `isUrlFallback`
2. `ParsedPage`: 宝塚 `{articles, dateFallbackCount}` / 四季 `{articles, matched, discarded, nextHref}` / 東宝 `{articles, matched}`
3. 全項目破棄の判定位置: 宝塚・東宝は `parsePage` 内、四季は `fetch()` 内
4. 破棄内訳の文字列化: 四季だけ `summarizeDiscarded()`、他はインライン
5. `parseDate` のシグネチャと走査方法（宝塚 `($item, url)`、四季 `($item)` `filter("[datetime]").first()`、東宝 `($, $item)` 全 `time` 走査）
6. 見出し取得: 宝塚 `extractTitle($link)`、四季 `extractTitle` + `extractItemTitle`、東宝 `extractTitle($item, $link)` に統合
7. `fetch()`: 東宝の「複数 URL の 0 件判定」は `sources[]` が複数のあらゆる Source に必要な規則 → 共通側の候補
8. コンストラクタ検証: 東宝のみ `selectItemSelector` の fail-fast
9. import 順（`matchCategoryKeywords` / `matchCategoryTags`）が東宝だけ逆
共通化の方針案: 完全同文の群を `domain`（`toHttpUrl`・`ymdFromMatch` 等の純粋関数）と `infrastructure/html/`（`classify`・`parsePage` 骨格）に分け、乖離は「宝塚の URL 予備経路」「四季のページ送り」「東宝の複数 URL 判定」を Source 側フックとして残す。`siteTags` の `.map().get()` → `.toArray().map` は 3 ファイル同時に。想定外ホストの記事を application で warn する観測は D-02 改訂候補

