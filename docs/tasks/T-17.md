---
id: T-17
title: app スキャフォールドと CI
stack: app
features: []
depends_on: [T-02]
status: review
branch: task/T-17
worktree: ../CurtainCall-T-17
pr:
---

## 内容

D-04 §10 **T-A app スキャフォールドと CI** を実装する。

- 成果物: `flutter create --platforms ios --org jp.curtaincall app`（生成物を整理し `uses-material-design: false`）、`pubspec.yaml`（§4.8）、`analysis_options.yaml`、`build.yaml`、`lib/main.dart`、`lib/app/app.dart`（`CupertinoApp` に仮のプレースホルダー）、`lib/core/logging/app_logger.dart`、`lib/core/network/feed_config.dart`、`lib/core/network/user_agent_client.dart`、`.github/workflows/app-ci.yml`。計 10
- 設計書側の依存: D-01 Task A（`app/assets/companies.json`）

## 完了条件

- [x] `flutter analyze` 警告ゼロ。`flutter test` は対象無しのため CI の step を `if: hashFiles('app/test/**/*_test.dart') != ''` で条件付きにし、T-B1 で外す
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-A   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §3.2・§4.8・§5.1（D-01 §10 Task C の契約実装を含む）
- docs/screens/S-00.md
- docs/requirements.md §5・§6（基盤）
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`app/`、`.github/workflows/`

## dev-loop 結果

## dev-loop 完了

タスク: T-17 app スキャフォールドと CI
対象: app
周回: 第1段階 3 周＋追加 1 周（FAIL レビュアーのみ）/ 第2段階 2 周
検証: PASS（flutter analyze / dart analyze --fatal-infos。flutter test は対象なし）

### 変更ファイル
- app/（`flutter create --platforms ios --org jp.curtaincall` の生成物を整理）、`pubspec.yaml`（sdk ^3.13.0 / flutter >=3.47.0、flutter_localizations、riverpod 3.0.3 系、drift、very_good_analysis）
- app/analysis_options.yaml（very_good_analysis + `plugins: riverpod_lint: 3.1.9` 完全一致固定。custom_lint は使わない）
- app/build.yaml（drift `store_date_time_values_as_text`）
- app/lib/main.dart、app/lib/app/app.dart（CupertinoApp プレースホルダー）、app/lib/core/logging/app_logger.dart、app/lib/core/network/feed_config.dart、user_agent_client.dart
- app/ios/Podfile（`MIN_IOS = '16.0'`、post_install で Pod の deployment target を引き上げ）、project.pbxproj（16.0、`TARGETED_DEVICE_FAMILY = 1`）、Info.plist（CurtainCall、Portrait のみ）
- .github/workflows/app-ci.yml（checkout / flutter-action を SHA 固定、flutter 3.47.4、`flutter analyze` → `dart analyze --fatal-infos` → `flutter test`（hashFiles ガード））、.gitignore

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 11 | Deployment Target 15→16、UA・feedTimeout の設計書一致、CI の SHA 固定、Info.plist、pubspec 整理ほか |
| 1-2 | 4 | riverpod_lint を `plugins:` 方式へ（custom_lint 削除）、TARGETED_DEVICE_FAMILY=1、Podfile 条件付き引き上げ、checkout SHA |
| 1-3 | 1 | **`flutter analyze` は analyzer プラグインを実行しない**ことを実証。CI に `dart analyze --fatal-infos` を追加 |
| 1-追加 | 3 | riverpod_lint を 3.1.9 に完全一致固定、Podfile `Gem::Version.correct?` ガード、CI コメント |
| 2-1 | 7 | analysis_options 注記を CI コマンドに一致、build.yaml 理由コメント、Podfile `MIN_IOS` 定数化、各コメント整理 |
| 2-2 | 0 | 2 人 PASS |

### 残っている「低」の指摘
- riverpod 同時更新コメントを dev_dependencies 側（riverpod_generator）にも置く
- Flutter 版が CI（3.47.4 固定）と pubspec（範囲）の 2 箇所。`.fvmrc` 等で一元化するのは別タスク候補
- UA の `1.0` は pubspec version と別管理。D-05 で package_info_plus 導入時に検討
- `flutter analyze` と `dart analyze` の二重実行（意図はコメント済み）

### 対応しなかった指摘
- なし

### 開発者に確認してほしいこと
- **CLAUDE.md「品質ゲート」の app 行**を `flutter analyze && dart analyze --fatal-infos && flutter test` に更新するか（riverpod_lint は `dart analyze` でしか動かない。verifier の実行コマンドも合わせる）。タスク外なので未変更

### D-04 §4.8 追随事項
1. `custom_lint` を dev_dependencies から削除。`riverpod_lint` は pubspec に置かず `analysis_options.yaml` の `plugins:`（3.1.9 完全一致）で有効化
2. `environment.sdk: ^3.13.0` / `flutter: ">=3.47.0"`（plugins 方式に必要な analyzer 版）
3. CI: `actions/checkout` / `subosito/flutter-action` を SHA 固定、`flutter-version: "3.47.4"` 明示、`dart analyze --fatal-infos` step を追加（`flutter analyze` はプラグインを実行しないため）
4. `flutter_localizations` を dependencies に追加（§5.9 由来）
5. `flutter test` の `hashFiles` 条件は暫定。T-18 でテスト追加後に削除（設計書には反映不要）

### 後続タスクへの申し送り
- T-18: `app-ci.yml` の `if: hashFiles(...)` 行を削除。最初の Provider を追加した周に `dart analyze --verbose` の出力で riverpod_lint の起動を一度確認する（CI ログだけではプラグイン未読込と問題なしを区別できない）
- riverpod / riverpod_generator の 4.x への更新は独立タスク（`chore(app)`）として起票候補

