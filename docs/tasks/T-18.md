---
id: T-18
title: articles の domain
stack: app
features: [F-11]
depends_on: [T-17]
status: review
branch: task/T-18
worktree: ../CurtainCall-T-18
pr:
---

## 内容

D-04 §10 **T-B1 articles の domain** を実装する。

- 成果物: `features/articles/domain/article.dart`・`category.dart`・`articles_file.dart`・`article_order.dart`・`article_sync_repository.dart`・`article_query_repository.dart`・`articles_feed.dart`・`read_display_state.dart`、`test/features/articles/domain/article_order_test.dart`・`read_display_state_test.dart`。計 10
- 設計書側の依存: T-A

## 完了条件

- [x] `flutter analyze && flutter test` が通る。`compareArticles`・`resolveReadDisplayState` の直接テスト（§7 純粋関数の例外）を含む。CI の `flutter test` の条件を外す
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-B1   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §3.2・§4.1・§4.3・§4.4・§7
- docs/screens/S-00.md
- docs/requirements.md §4.1 F-11
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/features/articles/domain/`、`test/features/articles/domain/`

## dev-loop 結果

## dev-loop 完了

タスク: T-18 articles の domain
対象: app
周回: 第1段階 2 周 / 第2段階 1 周（低のみ修正）
検証: PASS（flutter analyze / dart analyze --fatal-infos / dart format / flutter test 10 tests）

### 変更ファイル
- app/lib/features/articles/domain/article.dart / category.dart / articles_file.dart / article_order.dart / article_sync_repository.dart / article_query_repository.dart / articles_feed.dart / read_display_state.dart
- app/test/features/articles/domain/article_order_test.dart（6 ケース）/ read_display_state_test.dart（4 ケース）
- app/pubspec.yaml（`meta` を直接依存に追加）
- .github/workflows/app-ci.yml（`flutter test` の `hashFiles` ガードを削除）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 9 | `ignore_for_file` → `@immutable`（package:meta）、`dart format`、`runtimeType` 比較削除、例外の `toString`、DateTime isUtc / List 不変の doc、`updatedAt < publishedAt` の境界テスト、url doc |
| 1-2 | 0 | 5 人 PASS |
| 2-1 | 9（低） | isUtc コメントの集約、「D-04 §5.2 手順 n」表記統一、`FeedApplyPlan` 規則をクラス doc へ、TODO(T-18) 形式、テスト名、`buffer`、`@immutable` を sealed 基底へ |

### 残っている「低」の指摘
- 例外 `toString` が cause を無制限に埋め込む（infrastructure 側で cause に本文を渡さない規約で足りる）
- collector `article-order.ts` / `category.ts` との規則乖離を検知する仕組みがない。フィクスチャ導入タスク（`test/fixtures/articles.sample.json`）で「サンプルを compareArticles で並べたものと一致」「全 category が other 以外に写る」の契約テストを両側に置く
- `Article` のフィールド追加時に `copyWith`・`==`・`hashCode` の更新漏れを検知できない（クラス doc に注意書き済み）

### 対応しなかった指摘
- なし

### D-04 追随事項
1. §3.2 `articles_file.dart` の説明を §4.1 に合わせる（`ArticlesFile（generatedAt・articles）・定数・isImplausibleGeneratedAt`。schemaVersion は定数と HttpArticlesFeed の検証で扱う）
2. §4.1・§4.3・§4.4・§4.7 の domain 値オブジェクトに `@immutable`（package:meta）を付ける方針を追記
3. §4.8 pubspec の dependencies に `meta`（`@immutable` 用）を追加
4. §4.7 `FeedOfflineException` / `FeedErrorException` に `toString` を追加

### 後続タスクへの申し送り
- T-20 以降（drift Repository・HttpArticlesFeed）: `Article` の DateTime は `isUtc == true` 前提。DB から読んだ値・パースした値は必ず `DateTime.utc` / `toUtc()` で構築する（`==` は isUtc も比較する）
- browser feature: 開く前のスキーム検証（https/http のみ）を行う
- riverpod_lint の起動確認（`dart analyze --verbose`）は本タスクで Provider を書かなかったため未実施。最初に Provider を書くタスク（T-20 の DI）で行う

### 開発者に確認してほしいこと
- なし

