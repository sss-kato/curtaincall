---
id: T-19
title: companies / settings / notifications / saved の domain
stack: app
features: [F-06, F-08]
depends_on: [T-17]
status: done
branch: task/T-19
worktree: ../CurtainCall-T-19
pr: https://github.com/sss-kato/curtaincall/pull/9
---

## 内容

D-04 §10 **T-B2 companies / settings / notifications / saved の domain** を実装する。

- 成果物: `features/companies/domain/company.dart`・`company_repository.dart`、`features/settings/domain/settings_repository.dart`・`browser_choice.dart`、`features/notifications/domain/push_gateway.dart`・`notification_tap.dart`、`features/saved/domain/saved_article_repository.dart`、`test/features/notifications/domain/notification_tap_test.dart`。計 8
- 設計書側の依存: T-A

## 完了条件

- [x] 同上。`parseNotificationTap` の直接テスト（§7 純粋関数の例外）を含む。T-B1 と並行可（触るディレクトリが互いに素）
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-B2   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §3.2・§4.4〜§4.6・§7
- docs/screens/S-00.md
- docs/requirements.md §4.1 F-06 / F-08
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/features/{companies,settings,notifications,saved}/domain/`、`test/features/notifications/domain/`

## dev-loop 結果

## dev-loop 完了

タスク: T-19 companies / settings / notifications / saved の domain
対象: app
周回: 第1段階 1 周 / 第2段階 2 周
検証: PASS（flutter analyze / dart analyze --fatal-infos / dart format / flutter test 19 tests）

### 変更ファイル
- app/lib/features/companies/domain/company.dart / company_repository.dart
- app/lib/features/settings/domain/settings_repository.dart（`SettingKeys`・`notificationPrefix`）/ browser_choice.dart（`fromValue` 追加）
- app/lib/features/notifications/domain/push_gateway.dart / notification_tap.dart
- app/lib/features/saved/domain/saved_article_repository.dart
- app/test/features/notifications/domain/notification_tap_test.dart（テーブル駆動 10 ケース）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 0 | 5 人 PASS |
| 2-1 | 7 | `SettingKeys` 3 キーの doc を「値 → 用途 → 書き手」に統一、`notificationPrefix` 定数、`BrowserChoice.fromValue`、doc 整理、テーブル駆動化＋空文字・List・bool ケース |
| 2-2 | 0 | 2 人 PASS |

### 残っている「低」の指摘
- `BrowserChoice.fromValue` の TODO 位置（doc の直上）
- T-20 で `SettingKeys.companyIdOf(String key)` を足して読み側の唯一の入口にすることを検討

### 対応しなかった指摘
- なし

### D-04 追随事項
1. §4.4 `BrowserChoice` に `fromValue(String? raw)`（行無し・未知は `inApp`。D-05 §4.3 の規則を domain へ）と `SettingKeys.notificationPrefix` を追記
2. §4.5 `Company` コード片に `// == / hashCode は 4 フィールドで定義（§8 #24）` を追記
3. §4.8 pubspec に `meta`（T-18 と同件）

### 後続タスクへの申し送り
- T-20（DriftSettingsRepository）: `notification.*` の LIKE と companyId 切り出しは `SettingKeys.notificationPrefix` を使う。`browserChoice()` は `BrowserChoice.fromValue` を使う

### 開発者に確認してほしいこと
- なし

