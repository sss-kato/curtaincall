---
id: T-20
title: drift と Repository・PushGateway 実装、基盤の DI
stack: app
features: [F-06, F-09]
depends_on: [T-18, T-19]
status: review
branch: task/T-20
worktree: ../CurtainCall-T-20
pr: https://github.com/sss-kato/curtaincall/pull/12
---

## 内容

D-04 §10 **T-C drift と Repository・PushGateway 実装、基盤の DI** を実装する。

- 成果物: `core/database/tables.dart`・`app_database.dart`（+ 生成物）、`features/articles/infrastructure/drift_article_repository.dart`（`ArticleSyncRepository` と `ArticleQueryRepository` の両方を implements。Provider は 2 本。§8 #42）、`features/saved/infrastructure/drift_saved_article_repository.dart`、`features/settings/infrastructure/drift_settings_repository.dart`、`features/companies/infrastructure/asset_company_repository.dart`、`features/notifications/infrastructure/noop_push_gateway.dart`、`core/di/providers.dart`（基盤・Repository・`pushGateway` の Provider。§4.7。UseCase の Provider は T-D・T-E が別ファイルに置く）、`test/helpers/in_memory_database.dart`。計 9
- 設計書側の依存: T-B1、T-B2

## 完了条件

- [x] `flutter analyze` 警告ゼロ。テストは T-D・T-E が UseCase 経由で行う
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-C   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §4.2・§4.4・§4.6・§4.7・§5.1・§7
- docs/screens/S-00.md
- docs/requirements.md §4.1 F-06 / F-09
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/core/{database,di}/`、`lib/features/*/infrastructure/`、`test/helpers/`

## dev-loop 結果

## dev-loop 完了

タスク: T-20 drift と Repository・PushGateway 実装、基盤の DI
対象: app
周回: 第1段階 2 周 / 第2段階 2 周
検証: PASS（build_runner / flutter analyze / dart analyze --fatal-infos / dart format / flutter test 19。riverpod_lint の起動を probe で確認済み）

### 変更ファイル
- app/lib/core/database/tables.dart（4 テーブル、`@DataClassName('ArticleRow')`）、app_database.dart（schemaVersion 1、インデックス、`upsertSetting` / `deleteSetting` / `readSetting`）+ 生成物
- app/lib/features/articles/infrastructure/drift_article_repository.dart（`ArticleSyncRepository` + `ArticleQueryRepository`。`applyFeed` はトランザクション）
- app/lib/features/saved/infrastructure/drift_saved_article_repository.dart、settings/infrastructure/drift_settings_repository.dart、companies/infrastructure/asset_company_repository.dart（schemaVersion・id・fcmTopic・重複の検証）、notifications/infrastructure/noop_push_gateway.dart
- app/lib/core/di/providers.dart（基盤・Repository・`pushGateway`）+ 生成物
- app/test/helpers/in_memory_database.dart

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 11 | **中: DateTime を UTC 正規化せず書き込み、ローカル時刻で `ORDER BY saved_at` が壊れる（実測）**、`!` の理由コメント無し、`jsonDecode` の暗黙 dynamic、`customUpdate` の `updates` 不一致、`prefer_initializing_formals` の誤った ignore、`notificationSettings` の再フィルタ、`watchCount` の distinct、settings upsert の集約、id/fcmTopic の書式検証 |
| 1-2 | 0 | 5 人 PASS |
| 2-1 | 9 | **中: Companion の二重写像**（`copyWith` 派生に）、手順 6 の 3 規則コメント、`_supportedSchemaVersion`、`readSetting`、TODO 形式、Provider doc、async 様式、`_enabledValue`、fcmTopic 64 字・重複検査 |
| 2-2 | 0 | 2 人 PASS |

### 残っている「低」の指摘
- `upsertSetting` の doc と TODO の二重記述、`_updateCompanion` の `///`、`_readSetting` の単純委譲
- `AssetCompanyRepository` の検証規則 6 件は UseCase テストで未検証（companies の UseCase タスクで `StateError` 4 ケースをテーブル駆動で）
- verifier が `dart format lib test` で生成物 `app_database.g.dart` を整形した。CI は format を実行しないため問題ないが、`dart format` の対象から `**/*.g.dart` を外す運用を検討
- インデックスを `@TableIndex` に移すのは schemaVersion 2 を上げるとき

### 対応しなかった指摘
- `updates` に端末に無い id が含まれても無言で無視される（`SyncCoordinator` が直列化するため現状維持）

### D-04 追随事項
1. §4.2 `Articles` に `@DataClassName('ArticleRow')`（domain `Article` との衝突回避）。`AppDatabase` に `upsertSetting` / `deleteSetting` / `readSetting`（settings の共通 key-value プリミティブに限定する方針を §8 に）
2. §4.4 `SettingKeys.notificationPrefix` と `notificationSettings()` の写像規則（`startsWith` 再フィルタ・空 companyId 除外。T-19 と同件）
3. §5.2 手順 8-5 の `customUpdate` の `updates` は `{articles, read_states}`
4. §3.1 依存表に `articles/infrastructure → settings/domain`（`SettingKeys.feed*` の定数のみ）
5. §4.5 `AssetCompanyRepository` の検証（schemaVersion・id `^[a-z][a-z0-9_]{1,31}$`・fcmTopic 書式と 64 字・重複）
6. §4.2 のインデックスは次の schemaVersion で `@TableIndex` へ

### 後続タスクへの申し送り
- T-21〜（UseCase）: `test/helpers/in_memory_database.dart`（`NativeDatabase.memory()`、`addTearDown(db.close)`）を使う。`Article` の DateTime は Repository が UTC 正規化する
- `bootstrap_overrides`: `driftDatabase` の既定パス（Application Support）を維持
- T-G（FCM）: `providers.dart` の `TODO(T-G)` で `FcmPushGateway` の分岐

### 開発者に確認してほしいこと
- なし

