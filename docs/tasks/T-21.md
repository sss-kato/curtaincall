---
id: T-21
title: 配信の取得と SyncArticlesUseCase・SyncCoordinator
stack: app
features: [F-05, F-10, F-11]
depends_on: [T-20]
status: review
branch: task/T-21
worktree: ../CurtainCall-T-21
pr:
---

## 内容

D-04 §10 **T-D 配信の取得と SyncArticlesUseCase・SyncCoordinator** を実装する。

- 成果物: `features/articles/infrastructure/articles_file_codec.dart`・`http_articles_feed.dart`、`features/articles/application/sync_result.dart`・`sync_articles_use_case.dart`・`sync_coordinator.dart`、`core/di/articles_providers.dart`（§4.7。§8 #45）、`test/helpers/mock_feed_client.dart`・`test_articles.dart`、`test/features/articles/application/sync_articles_use_case_test.dart`・`sync_coordinator_test.dart`、`test/fixtures/articles.sample.json`（collector からのコピー。数えない）。計 10
- 設計書側の依存: T-C

## 完了条件

- [x] `flutter analyze && flutter test` が通る。§7 の `SyncArticlesUseCase` の 7 group と `SyncCoordinator` の 5 group のケース（後退防止・世代番号・全体タイムアウトの完了通知・dispose を含む）をすべて含む
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-D   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §4.3・§4.4・§5.2・§5.3・§6・§7
- docs/screens/S-00.md, S-01.md
- docs/requirements.md §4.1 F-05 / F-10 / F-11
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/features/articles/{application,infrastructure}/`、`lib/core/di/articles_providers.dart`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-21 配信の取得と SyncArticlesUseCase・SyncCoordinator
対象: app
周回: 第1段階 2 周 / 第2段階 3 周
検証: PASS（flutter analyze / dart analyze --fatal-infos / dart format / flutter test 96 tests）

### 変更ファイル
- `app/lib/features/articles/infrastructure/articles_file_codec.dart`・`http_articles_feed.dart`（新規。ETag・`Accept`・`If-None-Match`。失敗分類は `SocketException → offline`、`IOException`（TLS 系）→ `httpStatus`、`ClientException → httpStatus`、`TimeoutException → timeout`、`FormatException` は `message`/`offset` のみログ。`HttpStatus` 定数）
- `app/lib/features/articles/application/sync_result.dart`・`sync_articles_use_case.dart`（新規。手順 0〜9。`_classify`・`_detectStaleFeed` を切り出し。sealed switch）・`sync_coordinator.dart`（新規。世代番号・直列化・後追い予約 1 回・全体タイムアウト・dispose。`_current` は名前付きレコード。完了通知は `!_disposed` のみで判定）
- `app/lib/core/di/articles_providers.dart` + `.g.dart`（新規）、`app/lib/core/network/feed_config.dart`（`syncOverallTimeoutMargin` を追加）
- `app/test/helpers/mock_feed_client.dart`（`MockFeedClient`・`feedResponse()`）・`test_articles.dart`（`articlesFileJson`・`testArticle`・`testArticleId`・`defaultGeneratedAt`）（新規。後続タスクで再利用）
- `app/test/features/articles/application/sync_articles_use_case_test.dart`（7 group）・`sync_coordinator_test.dart`（5 group。`fake_async`）（新規）
- `app/test/fixtures/articles.sample.json`（collector とバイト同一）、`app/pubspec.yaml`（`fake_async`）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 11 | TLS 系例外が `storage` に誤分類 → `on IOException` → `httpStatus`、失敗分類 12 ケースで DB 不変のテーブル駆動、applyFeed 途中失敗を実物ラッパ（UNIQUE 違反）で巻き戻し検証、`prefer_initializing_formals` ignore 撤去、テストの `!` 撤去、sealed switch、`Future.sync`、タイムアウト中の後追い予約テスト、`FormatException` ログ制限 |
| 1-2 | 0 | 5 人 PASS（低 4 → 第 2 段階へ持ち越し） |
| 2-1 | 10 | `_execute` 120 行を `_classify`・`_detectStaleFeed` に分割、`_timedOutGeneration` 削除、`testArticle`/`testArticleId` を helpers へ、`_buildEnv` に `articles` 注入、`feedResponse()` で 49 か所置換、seed の assert、`HttpStatus`、`syncOverallTimeoutMargin` |
| 2-2 | 3 | `_timedOutGeneration` 削除の根拠コメントを判断箇所へ、`defaultGeneratedAt` 定数、`overallTimeout` doc |
| 2-3 | 0 | readability PASS |

### 残っている「低」の指摘
- `_start` 末尾の根拠コメント 7 行を 5 行程度に圧縮（`Future.timeout` の標準動作の説明を削る）

### 対応しなかった指摘
- なし

### D-04 追随事項（設計書を reopen するときにまとめて反映）
- §4.3 分類表に「`SocketException` を継承しない `IOException`（`HandshakeException`・`TlsException`・`CertificateException`）→ `FeedErrorException(httpStatus)`（ネットワークはあるのでオフラインにしない。§8 #7）」を追加し、§6・§7 の列挙にも同ケースを加える。**追記後に `http_articles_feed.dart` の `TODO(T-21)` を削除**
- §4.3: `User-Agent` は `UserAgentClient` が付与（`HttpArticlesFeed` は `Accept` と `If-None-Match` のみ）。応答サイズ上限は未規定（多層化するなら `Content-Length`/受信バイト数 5 MB 等で `malformed`。`send()` + stream に置き換え）
- §5.3: `SyncStarted` / `SyncCompleted` に `==` / `hashCode`（§8 #24 の対象に `SyncEvent`）。完了通知は世代比較ではなく `!_disposed` のみ（pending 中は `_start` が呼ばれず、`Future.timeout` が遅れた結果を捨てるため）。`overallTimeout = feedTimeout + syncOverallTimeoutMargin`（`core/network/feed_config.dart`）
- §4.8 dev_dependencies に `fake_async`
- §7: `MockFeedClient` は `http.Response.bytes(utf8.encode(...))`（既定 Latin-1 で日本語が壊れる）。`test/helpers/test_articles.dart` に `testArticle` / `testArticleId` / `defaultGeneratedAt`、`mock_feed_client.dart` に `feedResponse()`。「失敗の分類」に `HandshakeException`、「全体タイムアウト」に後追い予約ケース、「applyFeed 途中失敗」は UNIQUE 違反注入

### 後続への申し送り
- T-22 以降: `MockFeedClient`・`feedResponse()`・`testArticle()`・`testArticleId()`・`defaultGeneratedAt` を再利用。`_insertLocalArticle` 等の直接 INSERT ヘルパは T-26（`seed_articles.dart`）完了後に置き換える
- D-05 `OpenArticleUseCase`（T-32）のレビューで `uri.scheme` が http/https かつ host 非空の検証とテストを必ず確認

### 開発者に確認してほしいこと
- なし

