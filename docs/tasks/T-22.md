---
id: T-22
title: 通知の UseCase と Coordinator
stack: app
features: [F-07, F-08]
depends_on: [T-20]
status: review
branch: task/T-22
worktree: ../CurtainCall-T-22
pr:
---

## 内容

D-04 §10 **T-E 通知の UseCase と Coordinator** を実装する。

- 成果物: `features/notifications/application/sync_push_subscriptions_use_case.dart`・`push_subscription_coordinator.dart`・`push_subscription_sync_result.dart`・`request_notification_permission_use_case.dart`、`features/notifications/presentation/notification_tap_providers.dart`、`core/di/notifications_providers.dart`（§4.7。§8 #45）、`test/helpers/fake_push_gateway.dart`、`test/features/notifications/application/sync_push_subscriptions_use_case_test.dart`・`push_subscription_coordinator_test.dart`・`request_notification_permission_use_case_test.dart`。計 10
- 設計書側の依存: T-C

## 完了条件

- [x] 品質ゲート通過。§7 の `SyncPushSubscriptionsUseCase`・`RequestNotificationPermissionUseCase` の各 1 group と `PushSubscriptionCoordinator` の 3 group（失敗 = 例外の再スローを含む）のケースをすべて含む。T-D と並行可（触るファイルが互いに素。`core/di/` は別ファイルと別の生成物）
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-E   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §4.6・§5.6・§5.7・§7
- docs/screens/S-01.md, S-03.md
- docs/requirements.md §4.1 F-07 / F-08
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/features/notifications/{application,presentation}/`、`lib/core/di/notifications_providers.dart`、`test/`

## dev-loop 結果

## dev-loop 完了

タスク: T-22 通知の UseCase と Coordinator
対象: app
周回: 第1段階 3 周 / 第2段階 2 周
検証: PASS（build_runner / flutter analyze / dart analyze --fatal-infos / dart format / flutter test 124 tests）

### 変更ファイル
- `app/lib/features/notifications/application/{push_subscription_sync_result,sync_push_subscriptions_use_case,push_subscription_coordinator,request_notification_permission_use_case}.dart`（新規。`PushSubscriptionSyncResult` は `==`/`hashCode`（`ListEquality`）と `List.unmodifiable`。UseCase は `{required this._x}`。Coordinator は `final class`、例外を再スロー、予約 1 回、`_start().then(reserved.complete, onError: reserved.completeError)`）
- `app/lib/features/notifications/presentation/notification_tap_providers.dart` + `.g.dart`、`app/lib/core/di/notifications_providers.dart` + `.g.dart`（新規。D-04 §4.7・§5.7 のコード片どおり）
- `app/lib/features/articles/application/sync_coordinator.dart`（`final class` に。T-21 との整合）
- `app/test/helpers/fake_push_gateway.dart`・`completer_stub.dart`（新規。`CompleterStub<T>` と `SyncExecutorStub`。`sync_coordinator_test.dart` も使用）
- `app/test/features/notifications/application/*_test.dart`（新規。購読の同期 9・直列化 5・失敗 5・pending 5・許可の要求 5）
- `app/pubspec.yaml`（`collection` を直接依存に）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 10 | `PushSubscriptionSyncResult` の `==`/`hashCode`（D-04 §8 #24。中）、依存の private 化（中）、テストの `!`（中）、`List.unmodifiable`、前の結果が残る検証、`final class`、予約経路の例外転送、`notificationSettings()` 失敗の伝播ケース |
| 1-2 | 3 | 予約の再実行が失敗する経路のテスト（中）、`FakePushGateway.dispose()` の `tearDown`、フェイクの非同期 throw |
| 1-3 | 0 | adversarial PASS |
| 2-1 | 7 | `list` → `allCompanies`（中）、`SyncCoordinator` を `final class`、理由コメント、テスト名、同期 throw テスト、`CompleterStub` 共通化 |
| 2-2 | 0 | readability PASS |

### 残っている「低」の指摘
- `SyncExecutorStub` の doc「薄い派生」→「内包し記録を足したラッパー」

### 対応しなかった指摘
- なし

### D-04 追随事項（設計書を reopen するときにまとめて反映）
- §4.4 `PushSubscriptionSyncResult` のコード片を非 const + `List.unmodifiable` + `==`/`hashCode` 版に。§4.8 の dependencies に `collection`。§8 #24 に「`List` フィールドの比較は `package:collection` の `ListEquality`」
- §5.6「失敗時」・§8 #41 を「`loadAll` と `notificationSettings` の失敗はそのまま伝播」に
- §5.6 `PushSubscriptionCoordinator`: 予約経路は `_start().then(reserved.complete, onError: reserved.completeError)`。`run()` の戻り値は必ず await して捕捉する
- §6: T-36 の `FcmPushGateway.subscribe` に `.timeout()` を入れ `TimeoutException` を `failed` に落とす方針
- §7 `購読の同期` に `notificationSettings()` 失敗の伝播ケース（`SettingsRepository` は例外を投げるフェイク）、`失敗` に「予約の再実行が例外」「同期 throw」、`直列化` に「3 回目以降は同じ Future」。テストダブルに `test/helpers/completer_stub.dart`
- §8 #33・#41: `SyncCoordinator` / `PushSubscriptionCoordinator` の直列化骨格（約 25 行）は 3 つ目の Coordinator が要る時点で `core/async/SerialRunner<T>` に共通化

### 後続への申し送り
- T-26（companies）: `fcmTopic` の許容文字（`[a-zA-Z0-9-_.~%]+`）検証を `Company` 生成時に
- 6 団体目追加時は `sync_push_subscriptions_use_case_test.dart` の `companyIds` とテスト名の件数を更新
- T-23（骨格）: `AppLifecycleSync` は `PushSubscriptionCoordinator.run()` の戻り値を必ず await して捕捉する

### 開発者に確認してほしいこと
- なし

