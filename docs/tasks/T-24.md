---
id: T-24
title: 共通 Widget（状態表示）
stack: app
features: [F-09]
depends_on: [T-17]
status: review
branch: task/T-24
worktree: ../CurtainCall-T-24
pr:
---

## 内容

D-04 §10 **T-F2a 共通 Widget（状態表示）** を実装する。

- 成果物: `core/ui/status/full_loading_view.dart`・`refreshing_header.dart`・`empty_view.dart`・`offline_banner.dart`・`full_error_view.dart`・`transient_notice.dart`。計 6
- 設計書側の依存: T-A

## 完了条件

- [x] `flutter analyze && flutter test` が通る（新規テストは無し。Widget テストは書かない。§7）。`core/ui/status` が Flutter SDK 以外を import しないこと。T-B1・T-F1・T-F2b と並行可（触るディレクトリが互いに素）
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-F2a   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §5.9・§7
- docs/screens/S-00.md
- docs/requirements.md §4.1 F-09
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/core/ui/status/`

## dev-loop 結果

## dev-loop 完了

タスク: T-24 共通 Widget（状態表示）
対象: app
周回: 第1段階 3 周 / 第2段階 1 周
検証: PASS（flutter analyze / dart analyze --fatal-infos / dart format / flutter test 95 tests。新規テストなし）

### 変更ファイル
- `app/lib/core/ui/status/{full_loading_view,refreshing_header,empty_view,offline_banner,full_error_view,transient_notice}.dart`（新規。S-00/E-20〜E-25。`FullErrorKind { error, offline }`。`TransientNotice.show(context, message:)` は `Overlay` に挿入し 3 秒で自動除去、static `_currentEntry` で最新 1 枚だけ残す、`remove()..dispose()`。色は `CupertinoColors` の動的色のみ、文字サイズは `textTheme` の既定。import は `flutter/cupertino.dart` と `dart:async` のみ）
- `app/lib/core/ui/status/centered_status_body.dart`（新規。`EmptyView`/`FullErrorView` 共通の中央配置。`SingleChildScrollView` で Dynamic Type 最大でも溢れない）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 6 | `OverlayEntry` の `dispose()` 漏れ（高）、最新 1 枚だけ残す、`CenteredStatusBody` 切り出し、`Semantics(button)` の二重解消、`label` 色の冗長指定削除、背景色を `secondarySystemBackground` に |
| 1-2 | 3 | `show()` で差し替えた直後に旧 Timer が発火する二重 `remove` の競合（中。`identical` ガード）、doc、`SingleChildScrollView` |
| 1-3 | 0 | flutter-expert PASS |
| 2-1 | 4 | 配置前提の doc、`title` → `message`、退避・`late final` の理由コメント |

### 残っている「低」の指摘
- なし

### 対応しなかった指摘
- なし

### D-04 追随事項
- §5.9 `TransientNotice`: static で最新 1 枚だけ残す（3 秒以内の連続呼び出しで重ねない）。`context` は `CupertinoTabView` 配下（配置画面）のものを渡す。`message` は固定文言のみ
- §5.9: `EmptyView`/`FullErrorView` は `CenteredStatusBody`（`core/ui/status/`）を共有。画面本体（有限の高さ制約）に直接置く
- S-00 §7.8 の帯の色は `secondarySystemBackground`（「システム背景色」の語彙どおり）

### 後続への申し送り
- T-33（ホーム画面）: `TransientNotice.show` には `HomeScreen` の `context` を渡す。`EmptyView` の文言は S-00 の固定文言
- `TransientNotice._currentEntry` の static 可変状態は将来 `TransientNoticeController` を Riverpod Provider で供給する形に置き換える候補

### 開発者に確認してほしいこと
- なし（余白・角丸・アイコン 48px は Cupertino の慣例。S-00・D-04 に規定なし）

