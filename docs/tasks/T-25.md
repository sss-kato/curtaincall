---
id: T-25
title: 共通 Widget（記事セル）
stack: app
features: [F-02, F-05, F-11]
depends_on: [T-18]
status: review
branch: task/T-25
worktree: ../CurtainCall-T-25
pr:
---

## 内容

D-04 §10 **T-F2b 共通 Widget（記事セル）** を実装する。

- 成果物: `core/ui/article/article_cell.dart`・`article_cell_model.dart`・`category_icon.dart`・`published_date_format.dart`、`test/core/ui/article/published_date_format_test.dart`。計 5
- 設計書側の依存: T-B1

## 完了条件

- [x] `flutter analyze && flutter test` が通る。`core/ui/article` の import が `articles/domain` と Flutter SDK だけであること。`formatPublishedDate` の直接テスト（§7。未来日を含む）を含む。T-F1・T-F2a と並行可（触るディレクトリが互いに素）
- [x] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [x] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-F2b   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §5.5・§5.9・§7
- docs/screens/S-00.md
- docs/requirements.md §4.1 F-02 / F-05 / F-11
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/core/ui/article/`、`test/core/ui/article/`

## dev-loop 結果

## dev-loop 完了

タスク: T-25 共通 Widget（記事セル）
対象: app
周回: 第1段階 3 周 + 追加 1 周 / 第2段階 2 周
検証: PASS（flutter analyze / dart analyze --fatal-infos / dart format / flutter test 106 tests。`TZ=UTC` でも通過）

### 変更ファイル
- `app/lib/core/ui/article/article_cell.dart`（新規。S-00/E-10〜E-17。`_HeaderRow`（E-11・E-12・E-16）・見出し・`_FooterRow`（E-14・E-17）・`_Thumbnail`（`SizedBox(width) > AspectRatio(1) > ClipRRect`、`Image.network` + `cacheWidth`、既読は `Image.opacity`、http/https 以外はプレースホルダー）。定数 `_thumbnailSize = 84`・`_readOpacity = 0.5`・`_cellPadding`・`_itemGap`・`_cornerRadius`）
- `app/lib/core/ui/article/article_cell_model.dart`（新規。4 フィールド `==`/`hashCode`。`flutter/foundation.dart` の `@immutable`）
- `app/lib/core/ui/article/category_icon.dart`（新規。S-00 §7.2 の 7 アイコン。`size` は `textStyle.fontSize`）
- `app/lib/core/ui/article/published_date_format.dart`（新規。`formatPublishedDate(publishedAt, now)`。両引数を内部で `toLocal()`、暦日差は `DateTime.utc` で計算（DST 対策）、未来日も §7.3 を機械的に適用）
- `app/test/core/ui/article/published_date_format_test.dart`（新規。テーブル駆動 9 + 単独 1）

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 8 | `AspectRatio` の無制約でレイアウト例外（重大。3 人が Widget テストで再現）、`url!`（高）、DST で暦日が 1 日ずれる（中）、`CategoryIcon` の高さ（中）、テーマ由来スタイル、`meta` → `foundation`、http/https 検証 |
| 1-2 | 5 | 年比較の `toLocal()` 漏れ（中。2 人が再現）、テスト名、`validUrl`、空白のみの見出し |
| 1-3 | 1 | 追加テストが UTC ランナーで失敗（中。**3 周打ち切り → 推奨案で追加 1 周**） |
| 1-4 | 0 | flutter-expert / adversarial PASS |
| 2-1 | 5 | `build` 90 行を `_HeaderRow`/`_FooterRow` に分割（中）、両引数を内部で `toLocal()`（中）、`_readOpacity` 定数（中）、リポジトリ外参照のコメント削除（中）、定数化・日付の明示 |
| 2-2 | 0 | readability PASS |

### 残っている「低」の指摘
- 呼び出し側 `formatPublishedDate(publishedAt.toLocal(), now)` の `.toLocal()` は冗長（関数内で変換）
- `4`・`12`・`0.5` のレイアウト値が定数化されていない
- テストコメント「UTC でも壊れない」の意図（CI が UTC の場合はこのテストで退行を検知できない）

### 対応しなかった指摘
- なし

### D-04 追随事項（設計書を reopen するときにまとめて反映）
- §5.9 `formatPublishedDate(DateTime publishedAt, DateTime now)`: 両引数を内部で `toLocal()`、暦日差は UTC 日付で計算（DST 対策）
- §5.9 E-15: `SizedBox(width: 84) > AspectRatio(1) > ClipRRect` の順（`AspectRatio` が外側だと `ListView` 内で無制約）。既読は `Image.opacity`。`Uri.tryParse(url)?.scheme` が http/https 以外はプレースホルダー（多層防御。検証責務は `ArticlesFileCodec`）。`cacheWidth` のみ指定だと横長 OGP 画像が cover で約 1.9 倍に拡大され粗くなる（実機で目立つなら 2 倍程度に取る方針を決める）
- §5.9 E-12: `CategoryIcon` の `size` は `textStyle.fontSize`（E-11 と同じ高さ）
- §10 T-F2b 完了条件「Flutter SDK」に `foundation.dart` の `immutable` を含む
- §7: `published_date_format_test.dart` は TZ 非依存にする（CI は UTC）。DST の実検証は `TZ=America/New_York flutter test test/core/ui/article/` を CI に足すか開発者判断

### S-00 追随事項（`/screen-doc reopen S-00` のときに）
- §3.2 / §7.6: E-15 の 1 辺 84pt
- §3.2: 「1px の区切り線」→「iOS 標準のリスト区切り（0.5pt）」

### 後続への申し送り
- T-33 / T-34: `buildArticleCellModel`（D-05 §5.12）は `ArticleCellModel` の 4 フィールドを埋めるだけ。`now` は `minuteClockProvider` から。`_thumbnailSize` は private のままでよい

### 開発者に確認してほしいこと
- なし

