---
id: T-36
title: FCM 実装（フェーズ 5、Apple Developer Program 加入後）
stack: app
features: [F-07, F-08]
depends_on: [T-23]
status: todo
branch:
worktree:
pr:
---

## 内容

D-04 §10 **T-G FCM 実装（フェーズ 5、Apple Developer Program 加入後）** を実装する。

- 成果物: `features/notifications/infrastructure/fcm_push_gateway.dart`、`pubspec.yaml` に `firebase_core`・`firebase_messaging`、`core/di/bootstrap_overrides.dart` の `fcm` 分岐（`Firebase.initializeApp()`）、`core/di/providers.dart` の `pushGateway` の `fcm` 分岐、`ios/Runner` の Push Notifications / Background Modes（remote-notification）の capability、`.gitignore` に `GoogleService-Info.plist`（§8 #27。開発者が手元の `app/ios/Runner/` に置く）。計 1 + 改修 5
- 設計書側の依存: T-F1、要件 §10 フェーズ 5

## 完了条件

- [ ] 品質ゲート通過（`none` ビルド）。`--dart-define=CURTAINCALL_PUSH=fcm` の実機ビルドで、collector（D-02）からの通知が届き、タップで `companyId` が `notificationTapStreamProvider` に流れる。同じ実機ビルドで S-01/ST-18（初回起動の許可ダイアログ）と S-03/ST-03（許可を拒否したときの案内行）を目視で確認する（§8 #29）。新規テストは無し（`FakePushGateway` で UseCase を、`parseNotificationTap` を純粋関数テストで検証済み）
- [ ] D-04 §7 に列挙したこのタスク分のテスト（ファイル・group 名）がすべてある
- [ ] 品質ゲートを通過している（`flutter analyze && flutter test`）

## 参照

- docs/design/D-04.md §10 T-G   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §4.6・§5.6・§5.7・§8 #27
- docs/screens/S-01.md, S-03.md
- docs/requirements.md §4.1 F-07 / F-08
- CLAUDE.md（app 節・テスト方針）

## 触るファイルの見込み

`lib/features/notifications/infrastructure/`、`lib/core/di/`、`app/ios/`

## dev-loop 結果

