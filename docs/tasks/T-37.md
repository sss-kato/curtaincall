---
id: T-37
title: collector のテスト方針の例外に GitArticlesPublisher を加える
stack: docs
features: []
depends_on: []
status: todo
branch:
worktree:
pr:
---

## 内容

`fix/publish-staged-diff`（publish 障害のホットフィックス）で `collector/test/infrastructure/storage/git-articles-publisher.test.ts` を追加したが、これは承認済みの設計書と規約が**名指しで対象外にしているクラス**のテストであり、乖離が残っている。

- `docs/design/D-02.md` §7.3：「`main.ts`・`ConsoleLogger`・`SystemClock`・**`GitArticlesPublisher`**・`FirebaseNotificationGateway` の単体テストは書かない。例外として `FetchHttpClient` と `ArticlesFileStore` の 2 クラスは…」
- `CLAUDE.md` テスト方針：例外の対象を「`FetchHttpClient`・`ArticlesFileStore`」の閉じた列挙で書いている

テストを追加した判断自体は spec-reviewer も「妥当」としている。`execFile` を丸ごとスタブ化すれば実 git を一切起動せずに検証でき、§7.3 が除外理由としていた「git を実行するコードはテストで呼ばない」という前提がここでは成立しないため。

問題は**乖離を残したまま確定させること**で、将来の開発者が §7.3 を根拠にこの回帰テストを削除し、同じ盲点（記事が 1 件でも変われば publish が必ず失敗する）が復活しうる。

## 完了条件

- [ ] `CLAUDE.md` のテスト方針の例外に `GitArticlesPublisher` を追加する（**`execFile` をスタブ化し実 git を起動しないこと**を条件として明記）。**これは規約の変更なので、着手前に開発者の承認を得る**
- [ ] `/design-doc reopen D-02` → §7.3 の除外リストから `GitArticlesPublisher` を外し、§7.3 の表に 1 行（`git diff --cached --quiet` の終了コード 1 と `signal: null` の扱い）を追加 → `/design-review D-02` → `/design-doc approve D-02`
- [ ] `collector/src/infrastructure/storage/git-articles-publisher.ts` のクラス docstring から、追随が済んだ旨に合わせて T-37 への参照を外す
- [ ] `node docs/tools/render-design.mjs D-02` で HTML を再生成し、同じコミットに含める

## 参照

- docs/design/D-02.md §7.3（テスト方針）・§5.3 手順 3（publish）
- CLAUDE.md（テスト方針・collector 節）
- PR: publish 障害のホットフィックス（`fix/publish-staged-diff`）

## 触るファイルの見込み

`CLAUDE.md`、`docs/design/D-02.md`、`docs/design/html/`、`collector/src/infrastructure/storage/git-articles-publisher.ts`

## dev-loop 結果
