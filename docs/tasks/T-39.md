---
id: T-39
title: app 基盤の追随（判定・共通 Widget・自動リトライ）
stack: app
features: [F-01, F-02, F-05, F-10, F-11]
depends_on: [T-23, T-24, T-28]
status: in_progress
branch: task/T-39
worktree: ../CurtainCall-T-39
pr:
---

## 内容

画面定義書 S-00〜S-03 の approved 化（2026-09-24）に伴い D-04 が改訂され、実装済みコード（T-23・T-24）との差分が生じた。その差分を引き取る。

主な変更は 3 つ。

1. **件数の写像を 4 値にする**：`bool? resolveHasArticles` → `ArticleCountState { pending, unavailable, empty, present }` を返す `resolveArticleCount(count, {required bool retryRequested})`。S-00/ST-17「記事を読み出せない（件数そのものが分からない）」を表すため
2. **判定表を 10 行にする**：先頭行で `unavailable` を拾い、3 行目で件数と一覧の更新順のずれを吸収する
3. **再試行を 2 系統に分ける**：ST-14 / ST-16 は取得をやり直す、ST-17 は端末の読み出しだけをやり直す。`countRetryRequestedProvider.retry()` が `state = true` と `ref.invalidate(articleCountProvider)` を 1 メソッドで行う

`ref.invalidate` は直前値を捨てない（Riverpod の `copyWithPrevious` が両経路で値を引き継ぐ）ため、写像の入力に `retryRequested` を渡して「受動的な失敗では直前値を保つ／明示的な再試行では捨てる」を表す。詳細は D-04 §5.4.1〜§5.4.4。

## 完了条件

- [ ] 品質ゲートを通過している（`flutter analyze && dart analyze --fatal-infos && flutter test`）
- [ ] `resolveListStatus` の判定表 10 行 × 4 出力（`unavailable` の 8 通り、`empty` × `hasVisible = true` → `content`、再購読中 → `loading`、再購読成功で 0 件の 4 分岐）のテーブル駆動テストが通る
- [ ] `resolveArticleCount` のテスト（`retryRequested` の true / false を 1 組で比較するケースを含む）が通る
- [ ] `shouldLogCountError`・`shouldClearRetryRequested`（飛行中で直前値あり → false／`AsyncError` で直前値あり → false／決着した `AsyncData` → true）のテストが通る
- [ ] `_noRetry` の複製が残っていない（`noRetry` を `lib/core/di/no_retry.dart` から共有。grep で確認）
- [ ] `syncsOnLaunch` の残存が無い（grep で確認）

## 参照

- docs/design/D-04.md §10 T-F3   ← 起点
- docs/design/D-04.md §3.2（ファイル配置）/ §5.4.1〜§5.4.4（判定と再試行）/ §5.9（共通 Widget）/ §7（テスト）/ §8 #61〜#63・#65・#67
- docs/screens/S-00.md §5.2 ST-17 / §6 A-12 / §8 #36・#38
- docs/screens/S-01.md §8 #30・#31

## 触るファイルの見込み

`lib/core/ui/status/`、`lib/core/di/no_retry.dart`、`lib/features/articles/presentation/`、`lib/features/notifications/presentation/`、`lib/app/`、`test/features/articles/presentation/`

**実行時期**：T-28（D-05 T-H3）の後・T-33（D-05 T-M）の前。T-33・T-34 とは `lib/app/app_lifecycle_sync.dart` を両方が改修するため並行しない。

## dev-loop 結果
