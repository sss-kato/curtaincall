---
id: T-41
title: resolveAnchorId を純粋関数に切り出す
stack: app
features: [F-01]
depends_on: [T-28]
status: in_progress
branch: task/T-41
worktree: ../CurtainCall-T-41
pr:
---

## 内容

T-28 で実装した `AnchoredListView` は、位置保持のアンカーを決める走査規則を `_AnchoredListViewState` の中に持っている。D-05 §5.11.4 はこれを純粋関数 `resolveAnchorId` として切り出し、テーブル駆動テストで担保することを定めた（§8 #33）。実装済みコードからの切り出しだけを行う片付けタスク。

走査規則が State 側に重複して残らないこと（切り出したのに両方に実装がある状態にしないこと）が要点。

## 完了条件

- [ ] 品質ゲートを通過している（`flutter analyze && dart analyze --fatal-infos && flutter test`）
- [ ] `resolveAnchorId` の 7 ケース（D-05 §7）が通る
- [ ] 走査規則が `_AnchoredListViewState` に重複して残っていない（grep で確認）

## 参照

- docs/design/D-05.md §10 T-Q   ← 起点
- docs/design/D-05.md §5.11.4（アンカーの決め方）/ §7（`group('アンカーの付け替え')` 7 ケース）/ §8 #33
- CLAUDE.md「テスト方針」（純粋関数の例外。`resolveAnchorId` は列挙済み）

## 触るファイルの見込み

`lib/core/ui/list/`、`test/core/ui/list/`

**実行時期**：T-28 完了済みなのでいつでも実行できる。**D-04 §10 T-P とは触るファイルが重ならないため並行可**。

## dev-loop 結果
