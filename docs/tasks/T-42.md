---
id: T-42
title: seedArticles の契約テストを書く
stack: app
features: []
depends_on: [T-26]
status: review
branch: task/T-42
worktree: ../CurtainCall-T-42
pr: https://github.com/sss-kato/curtaincall/pull/38
---

## 内容

`test/helpers/seed_articles.dart` は `ArgumentError` 7 種と `StateError` 1 種の引数検証を持つが、**どのテストからも実行されていない**。このヘルパは T-29 以降のほぼ全 UseCase テストの前提を作る単一障害点で、壊れると誤った前提でテストが通ってしまう。

CLAUDE.md「テスト方針」に例外として「`test/helpers/` のテストヘルパ自身の契約テスト」を追加済み（開発者確認 2026-09-24）。`lib/` は触らず、テストファイル 1 つを足すだけ。

## 完了条件

- [x] 品質ゲートを通過している（**2026-09-24 に `dart format` が加わり 4 コマンド**。verifier で全 OK・217 tests）
- [x] `group('引数の検証')` 7 ケース（`ArgumentError`。いずれも **DB に触れる前**に投げること＝`articles` が 0 行のまま）が通る
- [x] `group('1 回だけ')` 1 ケース（`StateError`。0 件投入の 1 回目も数える／`ArgumentError` で弾かれた呼び出しは数えない）が通る

## 参照

- docs/design/D-05.md §10 T-R   ← 起点
- docs/design/D-05.md §7（`seedArticles` のヘルパ表とケース）/ §8 #36
- CLAUDE.md「テスト方針」（`test/helpers/` の契約テストの例外）

## 触るファイルの見込み

`test/helpers/` のみ（`lib/` は触らない）

**実行時期**：`test/helpers/` 以外を変更しないため**他のすべてのタスクと並行できる**。

## dev-loop 結果

## dev-loop 結果

タスク: T-42 seedArticles の契約テストを書く
対象: app
周回: 第 1 段階 2 周（spec / architecture / adversarial / security / flutter-expert）/ 第 2 段階 3 周（readability / maintainability）
検証: PASS（`flutter analyze` / `dart analyze --fatal-infos` / `flutter test` 217 tests / `dart format` 0 changed）

### 変更ファイル
- `app/test/helpers/seed_articles_test.dart`（新規 358 行）
- `app/test/helpers/seed_articles.dart`（doc コメントのみ 10 行）
- **`lib/` は不変**

テストは 15 件（`group('引数の検証')` の meta 1 + `ArgumentError` 12 + `group('1 回だけ')` 2）。
D-05 §7 の 7 ケース文を `caseText` に保ち、複数経路のケースは `branch` ごとに `test` を分けて fail isolation を確保している。

### 修正した指摘の要約
| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 7 | **11 ある throw 経路のうち 5 経路が未実行**（throw を消してもテストが緑のまま）。テーブル駆動で全経路を踏むように。「DB に触れる前に投げる」の確認が 1 ケースで空振りしていた。ヘルパ本体の doc が「テストは持たない」の旧方針のまま |
| 1-2 | 10 | 検証順への暗黙依存を除去。`settings` を含む 4 テーブルの確認に。`// ignore:` を連結位置の変更で撤去。命名と型の整理 |
| 2-1 | 14 | **`dart format` 未適用**（app 全 114 ファイル中このファイルだけ）。レビュー ID がコメントに残存。定型ブロックが 8 箇所にコピー。1 test に 3 契約が同居 |
| 2-2 | 7 | 2 段構造で `caseText` が未使用・二重管理に。**`group('1 回だけ')` が D-05 §7 と不一致**（司令塔の指示による退行）。1 段のフラットな表に平坦化して両方解消 |
| 2-3 | 4 | meta テスト名が表明より強い。冒頭コメントの重複と数の散在 |

### 残っている「低」の指摘
- `_buildArgumentErrorCases` 直前のコメントに「7 ケース」が 1 箇所残っている（数は meta テストに一本化する方針の取りこぼし。対応は任意）

### 対応しなかった指摘
- **flutter-expert「`completes` もクロージャ渡しに揃える」**: `completes` は `Future` しか受け付けず、クロージャを渡すと `was not a Future` で失敗する。**司令塔が実測で確認**し、architecture の反対意見が正しいと判定した
- maintainability の 3 件（`ArgumentError.value` への移行 / 行数カウンタの 1 本化 / `group('投入結果')` の追加）: レビュアー自身が「今回は対象外・将来」と明記

### この周で見つかった規約・設計書の問題（本タスクの外）
1. **品質ゲートに `dart format` が無かった** → 開発者確認のうえ CLAUDE.md・verifier.md・app-ci.yml に追加（`ddbf24a`）。あわせて **verifier.md に `dart analyze --fatal-infos` が抜けていた**のも修正
2. **D-05 §7 の擬似コードが async 関数では成立しない** → `expect(() => seedArticles(...), throwsArgumentError)` では失敗 Future を捕まえられず、**通るのに何も検証しない**。正しくは `await expectLater(...)`。**D-05 は approved のため「次回 reopen 時」の申し送りとして記録**
3. **§8 #36 の「12 経路」と §7 のケース列挙（7 行）の粒度が合っていない** → 素直に 1 行 1 テストで書くと 5 経路が漏れる。同じく次回 reopen 時の申し送り

### 開発者に確認してほしいこと
- なし（品質ゲートへの `dart format` 追加は 2026-09-24 に確認済み）

次のステップ:
1. この worktree で最終レビューと動作確認
2. 問題なければ `/pm pr T-42`
