---
id: T-46
title: RunSummary と 2 つの静かな停止の検知を D-02 に合わせる
stack: collector
features: [F-11]
depends_on: [T-45]
status: done
branch: task/T-46
worktree: ../CurtainCall-T-46
pr: https://github.com/sss-kato/curtaincall/pull/47
---

## 内容

D-02（approved、2026-09-25）§8.1 の実装追随 **(b)・(c) の残り・(f) の §7.1 分**。本タスク群で最も大きい。

現行の `RunSummary` は `published: boolean` のままで、D-02 が定めた `publishOutcome`（4 値）/ `publisher` / `survivingChanges` を持たない。そのため **2 つの「静かな停止」の検知が動いていない。**

**とくに `survivingChanges` は急ぐ。** D-02 §8 #51 のとおり、第 2 周まで採っていた `dropped === 0 && created + updated > 0` という条件は、団体の記事が 100 件（上限）に達すると新着 1 件につき末尾 1 件が必ず落ちるため**定常運用では一度も真にならない**。**本番の `data/articles.json` は既に宝塚が 100 件に達しており、この状態に入っている。**

実装するもの（D-02 の該当節が正）。

- `DetectDiffResult.stats.survivingChanges`（§4.6）と `§5.2 手順 7b` の算出（手順 7 と**同じ走査**で、新着**または更新**かつ切り詰め後に残っているものを数える。手順 8 の置き換えの影響を受けない）
- `RunSummary` を判別可能ユニオンに（§4.8）。`publishOutcome: "published" | "no_changes" | "skipped" | "unknown"`・`publisher: "git" | "noop"`・`survivingChanges`
- `buildFailureSummary(params)` を **4 引数**（`clock` / `error` / `publisherKind` / `notificationGatewayKind`）にし、`sourceFailures` の導出（`error instanceof SnapshotMissingError`）を**関数の中**へ寄せる（§8 #46。`main.ts` に `instanceof` を書かせない）。`ArticlesValidationError` も `"skipped"`（§8 #47）
- `sanitizeFailureMessage` / `MAX_FAILURE_MESSAGE_LENGTH` の `export` を外して非公開に（§8 #54）。切り詰めは**コードポイント単位**（§8 #55）
- `RunCollectionInput.dryRun` を追加し、手順 8 の判定をそこへ移す（§8 #45）。`publisherKind` / `notificationGatewayKind` はサマリ記録専用
- `main.ts` 手順 4b で `GITHUB_ACTIONS === "true" && CURTAINCALL_DRY_RUN === "1"` を**起動時に拒否**（終了コード 1。§8 #44）
- `main.ts` 手順 10 の carve-out：**手順 3〜6 の失敗では `buildFailureSummary` を呼ばずサマリを書かない**（§5.5）
- `RunCollection` 手順 7 の矛盾判定を `diff.stats.survivingChanges > 0` に、手順 5 のログを `{ failures: collect.failures.length }` に（§4.7 の `LogFields` はスカラーのみ）
- `.github/workflows/collect.yml` を §5.6 に同期（jq 5 か所の型ガードとフォールバック、本文サマリの `残存 \(.survivingChanges)`、`discardedByCompany` の `(.value | type) == "number"`、2 つの検知ステップ、`outputs`）
- **(c)**：`run-summary-store.ts:7` と `run-collection.ts:95` の `// D-02 追随` コメントを削除または通常の参照コメントに置き換える

> **重要（D-02 §8.1 (b) の制約）**：`RunSummary` の型変更（§4.8）と `collect.yml` の jq の書き換え（§5.6）は**必ず同一コミット**で入れる。片方だけ先に入ると `published` の判定が噛み合わず、Pages が更新されない、または毎時無駄にデプロイされる。

## 完了条件

- [x] `DetectDiffResult.stats.survivingChanges` があり、**切り詰めで落ちた新着は数えず、切り詰め後に残った更新は数える**
- [x] D-02 §7.1 の `detect-diff` の 4 ケースが通る（100 件の団体に新着 1 件 → `created:1`・`dropped:1`・`survivingChanges:1` ／ 新着が 101 番目に沈む → `survivingChanges:0` ／ **既存 1 件の `contentHash` が変わった → `created:0`・`updated:1`・`survivingChanges:1` で通知対象は 0 件** ／ 前回と同一 → すべて 0）
- [x] D-02 §7.1 の `run-collection`「差分判定側の静かな停止の検知」3 ケースが通る
- [x] `buildFailureSummary` が 4 引数で、`main.ts` に `instanceof SnapshotMissingError` が無い
- [x] D-02 §7.1 の `buildFailureSummary` のケース（`SnapshotMissingError` / `ArticlesValidationError` / その他 / **`publisher`・`notificationGateway` が渡した種別と一致**）が通る
- [x] Actions 上で `CURTAINCALL_DRY_RUN=1` のとき起動時に終了コード 1 で拒否する
- [x] `collect.yml` が D-02 §5.6 と一致する（jq の型ガード・`残存`・2 つの検知ステップ・`outputs`）
- [x] **`RunSummary` の型変更と `collect.yml` が同一コミットに入っている**
- [x] `grep -rn "D-02 追随" collector/src/application/run-collection.ts collector/src/infrastructure/storage/run-summary-store.ts` が空
  - **訂正（司令塔・2026-09-26）**：当初この条件は `collector/src/application` ディレクトリ全体を対象にしていたが誤り。`collect-articles.ts:37`・`:233`・`:277` の 3 件は D-02 §8.1 **(h) の 5 番目**（`RawArticle` の実行時形検査。§5.1 手順 6）に属し、**T-47 の管轄**。§8.1 (c) が列挙する 6 ファイルにも `collect-articles.ts` は入っていない。T-46 が担当するのは `run-collection.ts` と `run-summary-store.ts` の 2 か所
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §8.1「実装（T-37 の後続タスク）」の (b)・(c)・(f)   ← 起点
- docs/design/D-02.md §4.6（`DetectDiffResult`）/ §4.8（`RunSummary`・`buildFailureSummary`・`sanitizeFailureMessage`）
- docs/design/D-02.md §5.2 手順 7b・8 / §5.5 手順 4b・5・7・8・10 / §5.6（ワークフローの YAML）
- docs/design/D-02.md §6（異常系）/ §7.1（UseCase テスト）
- docs/design/D-02.md §8 #41・#44〜#47・#51・#53・#54・#55
- docs/requirements.md §4.1 F-11 / §5

## 触るファイルの見込み

`collector/src/application/`（`run-collection.ts`・`detect-diff.ts`）、`collector/src/main.ts`、`collector/src/infrastructure/storage/run-summary-store.ts`、`.github/workflows/collect.yml`、`collector/test/application/`

**T-45 の後**（`main.ts` を共有）。T-44・T-47 とは並行可だが、T-47 も `application/` を触る（コメントのみ）ため、同時に走らせるなら T-46 を先にマージする。

## dev-loop 結果

タスク: T-46 `RunSummary` と 2 つの静かな停止の検知を D-02 に合わせる
対象: collector
周回: 第1段階 2 周（5 レビュアー全員 PASS）＋ 低 7 件の適用／第2段階 3 周（2 レビュアー PASS）＋ 低 1 件の適用
検証: PASS（Node 22.23.2、`npm run lint` / `npx tsc --noEmit` / `npx vitest run` 503 tests）

### 変更ファイル

- `collector/src/application/detect-diff.ts`（`stats.survivingChanges`・手順 7b・`countKinds`・`assertNever`）
- `collector/src/application/run-collection.ts`（`RunSummary` の判別可能ユニオン・`buildFailureSummary`・`sanitizeFailureMessage` の非公開化）
- `collector/src/main.ts`（手順 4b の DRY_RUN 拒否・`isDryRun` の一元化）
- `collector/src/infrastructure/storage/run-summary-store.ts`（`D-02 追随` コメントの整理）
- `.github/workflows/collect.yml`（§5.6 への同期）
- テスト 2（`detect-diff.test.ts` / `run-collection.test.ts`）

**`RunSummary` の型変更と `collect.yml` は同一コミットに入っている**（D-02 §8.1 (b) の制約）。

### 実際に直った本番のバグ 2 件

1. **`survivingChanges` が無く、検知が `dropped === 0` 条件だった。** 団体の記事が 100 件（上限）に達すると新着 1 件につき末尾 1 件が必ず落ちるため、**定常運用で一度も真にならない**。本番の `data/articles.json` は既に宝塚が 100 件に達しており、**検知が死んでいた**（§8 #51）
2. **`created` / `updated` を切り詰め後の配列から数えていた。** 上限で落ちた新着が `created` に計上されず、D-02 §7.1 の「新着が `compareArticles` 順の 101 番目 → `created: 1`・`dropped: 1`・`survivingChanges: 0`」を満たせなかった（§5.5 手順 7 が「`created`・`updated` は切り詰めで落ちた新着を含む」と定めている）

### 修正した指摘の要約

| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | 0 | spec が指摘 0 で PASS（`collect.yml` と §5.6 の逐語一致を確認） |
| 1-2 | 2 中 + 低 | **無害化の 4 性質が未検証**（3 人が独立に指摘）／**手順 8 の置き換えが `survivingChanges` に影響しないことが未検証**（変異が生き残る） |
| 1-3（低の適用） | 7 | 手順 7/7b の走査を出力配列 `articles` の一巡に一本化／`countKinds` を `kindById` の 1 引数に／`assertNever` への統合／未知 `companyId` のテスト追加 ほか |
| 2-1 | 2 高 | **通知対象が「入る」側のテストが 1 件も無く、`set`/`push` を落とす変異が全テスト緑で通る**／コメントが「`created` に数えない」と述べているが数えている（同一ファイル内で矛盾） |
| 2-2 | 1 中 + 低 | **肯定側テストのデータが `companies` 順と `collected` 順を区別できない**（`sort` 除去も走査元の差し替えも捕まえられなかった） |
| 2-3（低の適用） | 1 | テストデータの url を順序非依存の名前に |

### 検証の要点

adversarial が **scratchpad 上に複製を作る方式**（worktree を汚さない）で延べ 30 近い変異を検証し、すべて kill されることを確認した。とくに次が効いている。

- **§7.1 の「100 件の団体で既存 1 件の `contentHash` が変わった」ケース**が、`survivingChanges` を「通知対象の件数」と同義に実装する変異を殺す
- 手順 7 の条件に `dropped === 0` を足す変異（#51 の退行）が殺される
- **`ArticleKind` に第 4 の種別を足すと `tsc` が 2 箇所でエラーになる**（`assertNever` による網羅性チェック）
- 肯定側テストは `list.push` → `list.unshift`・`companies` の反転・`case "updated"` の混入・走査元の `resolvedById` への差し替え・手順 5 の `sort` 除去をいずれも殺す

### 残っている「低」の指摘

なし。

### 対応しなかった指摘

いずれも **D-02 §5.6 の YAML そのものに含まれる内容**で、実装は設計書に忠実。`docs/tasks/FOLLOWUPS.md` の D-02 の節に 1-14〜1-19 として起票済み。

- jq の `gsub("[\r\n]"; " ")` が CR/LF しか潰さず、コメントの「同じ処理を掛ける」が事実と異なる（1-14）
- `.run-summary.json` が 0 バイトのとき jq が終了コード 0・出力なしで `unknown` に倒れない（1-15）
- `$GITHUB_OUTPUT` への複数文書出力（1-16）／ステップサマリの Markdown 地の文（1-17）
- `truncateCodePoints` の `domain/text.ts` への切り出し（1-18。**D-01 §3.2 の公開物の列挙への追随が要る**）
- `S=.run-summary.json` を `SUMMARY_JSON` にリネーム（1-19）

### 開発者に確認してほしいこと

なし（設計書への追随はすべて FOLLOWUPS に記録済み）。
