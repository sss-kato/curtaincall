---
id: T-47
title: 規定が入った実装コメントを整理する（http / logging / domain / application）
stack: collector
features: []
depends_on: []
status: todo
branch:
worktree:
pr:
---

## 内容

D-02（approved、2026-09-25）§8.1 の実装追随 **(g)・(h)**。**実装の挙動は変えない。** 設計書に規定が入ったことで不要になった自己申告コメントを片付けるだけのタスク。

D-02 の追加 1 周で、実装が「D-02 側の追随が必要」と自己申告していた乖離を**設計書側が実装に合わせる形**で取り込んだ（実装のほうが安全側だったため）。その結果、次のコメントが役目を終えている。

**(g) http 層（3 件）**

- `fetch-http-client.ts:222` の `// TODO(D-02 §8.1 申し送り): 上記の https→http 降格拒否とホスト変更時の warn を §5.7 / §8.1 に反映する` を**削除**（§5.7 手順 2b と §8 #58、および §4.3 の契約文に規定が入った）
- `sleep.ts` を指す但し書き（§3.2 に載った）
- `DEFAULT_FETCH_HTTP_CLIENT_OPTIONS` の「§5.7 のコード片には無い export だが」という但し書き（§5.7 の既定値表に載った）

**(h) logging / domain / application（5 系統）**

いずれも `D-02 側の追随が必要` と書かれている。規定が入ったので削除するか通常の参照コメントに置き換える。**実装は変えない。**

1. `infrastructure/logging/console-logger.ts:15-17` — 値が空文字、または空白・`=`・引用符・C0/C1 制御文字を含む場合に `JSON.stringify` で囲む（ログインジェクションと行崩れの防止）→ §4.7 に規定
2. `infrastructure/logging/console-logger.ts:43-45` と `domain/logger.ts:62-63` — `parseLogLevel` で環境変数由来の値を再検証し未知値を `info` へ縮退 → §4.7（公開物）・§4.9 に規定
3. `domain/logger.ts:29-31` — `formatError` の `cause` が循環参照・異常に深い連鎖・`String()` 不能でも安全に扱う → §4.7 に `MAX_CAUSE_DEPTH = 5` と `[unprintable]` として規定
4. `domain/datetime.ts:26-28` — `toJstDateTime` の年範囲チェック（`Date` は西暦 -271821..275760 を保持できる）→ §4.2 に規定
5. `application/collect-articles.ts:37`・`:233`・`:277` — `RawArticle` の実行時形検査と非配列の戻り値の扱い → §5.1 手順 6 に規定

## 完了条件

- [ ] `grep -rn "D-02 側の追随が必要" collector/src` が空
- [ ] `grep -rn "TODO(D-02" collector/src` が空
- [ ] **実装の挙動が変わっていない**（`git diff` にコメント以外の変更が無い。テストの追加・変更も不要）
- [ ] 残した参照コメントが D-02 の**実在する節番号**を指している（例：`§4.7`・`§4.9`・`§5.1 手順 6`）
- [ ] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §8.1「実装（T-37 の後続タスク）」の (g)・(h)   ← 起点
- docs/design/D-02.md §4.2（`toJstDateTime` の年範囲）/ §4.3（`getText` の契約と降格拒否）/ §4.7（`ConsoleLogger` の書式・`formatError`・`parseLogLevel`）/ §4.9（`CURTAINCALL_LOG_LEVEL`）
- docs/design/D-02.md §5.1 手順 6（`RawArticle` の実行時形検査）/ §5.7 手順 2b（リダイレクト検査・既定値表）
- docs/design/D-02.md §3.2（`sleep.ts`・`logger.ts` の公開物）/ §8 #58

## 触るファイルの見込み

`collector/src/infrastructure/http/`、`collector/src/infrastructure/logging/`、`collector/src/domain/`（`logger.ts`・`datetime.ts`）、`collector/src/application/collect-articles.ts`

**T-44・T-45 と並行可。** T-46 も `application/` を触るため、同時に走らせるなら T-46 を先にマージする（本タスクはコメントのみなので衝突しても解消は容易）。

## dev-loop 結果
