---
id: T-47
title: 規定が入った実装コメントを整理する（http / logging / domain / application）
stack: collector
features: []
depends_on: []
status: review
branch: task/T-47
worktree: ../CurtainCall-T-47
pr:
---

## 内容

D-02（approved、2026-09-25）§8.1 の実装追随 **(g)・(h)**。**実装の挙動は変えない。** 設計書に規定が入ったことで不要になった自己申告コメントを片付けるだけのタスク。

D-02 の追加 1 周で、実装が「D-02 側の追随が必要」と自己申告していた乖離を**設計書側が実装に合わせる形**で取り込んだ（実装のほうが安全側だったため）。その結果、次のコメントが役目を終えている。

**(g) http 層（3 件）**

- `fetch-http-client.ts:222` の `// TODO(D-02 §8.1 申し送り): 上記の https→http 降格拒否とホスト変更時の warn を §5.7 / §8.1 に反映する` を**削除**（§5.7 手順 2b と §8 #58、および §4.3 の契約文に規定が入った）
- `sleep.ts` を指す但し書き（§3.2 に載った）
- `DEFAULT_FETCH_HTTP_CLIENT_OPTIONS` の「§5.7 のコード片には無い export だが」という但し書き（§5.7 の既定値表に載った）

**(h) logging / domain / application（5 系統）**

いずれも `D-02 側の追随が必要` と書かれている。規定が入ったので削除するか通常の参照コメントに置き換える。**実装は変えない。**

1. `infrastructure/logging/console-logger.ts:15-17` — 値が空文字、または空白・`=`・引用符・C0/C1 制御文字を含む場合に `JSON.stringify` で囲む（ログインジェクションと行崩れの防止）→ §4.7 に規定
2. `infrastructure/logging/console-logger.ts:43-45` と `domain/logger.ts:62-63` — `parseLogLevel` で環境変数由来の値を再検証し未知値を `info` へ縮退 → §4.7（公開物）・§4.9 に規定
3. `domain/logger.ts:29-31` — `formatError` の `cause` が循環参照・異常に深い連鎖・`String()` 不能でも安全に扱う → §4.7 に `MAX_CAUSE_DEPTH = 5` と `[unprintable]` として規定
4. `domain/datetime.ts:26-28` — `toJstDateTime` の年範囲チェック（`Date` は西暦 -271821..275760 を保持できる）→ §4.2 に規定
5. `application/collect-articles.ts:37`・`:233`・`:277` — `RawArticle` の実行時形検査と非配列の戻り値の扱い → §5.1 手順 6 に規定

## 完了条件

- [x] `grep -rn "D-02 側の追随が必要" collector/src` が **(g)・(h) の 8 箇所について**空
  - **訂正（司令塔・2026-09-26）**：当初この条件は「`collector/src` 全体で空」としていたが誤り。`main.ts`（`errorName` フィールド）と `infrastructure/fcm/firebase-notification-gateway.ts`（3 箇所：`getMessaging(app)` への明示 App 渡し・`getApps().find(name === DEFAULT)` での絞り込み・throw 直前の `warn` 3 文言）の計 4 件は **T-45 が残した未解決の設計ギャップ**で、D-02 §8.1 (d) の残り分にあたる。**D-02 側に規定が入るまで外せない**（`docs/tasks/FOLLOWUPS.md` の 1-1・1-2・1-4・1-20 に起票済み）。T-47 の対象は本文に列挙した (g) 3 件・(h) 5 系統の計 8 箇所のみ
- [x] `grep -rn "TODO(D-02" collector/src` が空
- [x] **実装の挙動が変わっていない**（`git diff` にコメント以外の変更が無い。テストの追加・変更も不要）
- [x] 残した参照コメントが D-02 の**実在する節番号**を指している（例：`§4.7`・`§4.9`・`§5.1 手順 6`）
- [x] 品質ゲートを通過している（`npm run lint && npx tsc --noEmit && npx vitest run`）

## 参照

- docs/design/D-02.md §8.1「実装（T-37 の後続タスク）」の (g)・(h)   ← 起点
- docs/design/D-02.md §4.2（`toJstDateTime` の年範囲）/ §4.3（`getText` の契約と降格拒否）/ §4.7（`ConsoleLogger` の書式・`formatError`・`parseLogLevel`）/ §4.9（`CURTAINCALL_LOG_LEVEL`）
- docs/design/D-02.md §5.1 手順 6（`RawArticle` の実行時形検査）/ §5.7 手順 2b（リダイレクト検査・既定値表）
- docs/design/D-02.md §3.2（`sleep.ts`・`logger.ts` の公開物）/ §8 #58

## 触るファイルの見込み

`collector/src/infrastructure/http/`、`collector/src/infrastructure/logging/`、`collector/src/domain/`（`logger.ts`・`datetime.ts`）、`collector/src/application/collect-articles.ts`

**T-44・T-45 と並行可。** T-46 も `application/` を触るため、同時に走らせるなら T-46 を先にマージする（本タスクはコメントのみなので衝突しても解消は容易）。

## dev-loop 結果

タスク: T-47 自己申告コメントの片付け（D-02 §8.1 の (g)・(h)）
対象: collector
周回: 第1段階 2 周（spec・readability とも PASS）＋ 低 4 件の適用
検証: PASS（Node 22.23.2、`npm run lint` / `npx tsc --noEmit` / `npx vitest run` 533 tests）

### レビュアーの構成について

**差分がコメントのみ（実装の挙動が 1 行も変わらない）ため、spec と readability の 2 人で回した。** architecture / adversarial / security / typescript-expert には検査対象が無い（CLAUDE.md の第 1 段階 5 人体制からは外れる。司令塔の判断）。

### 変更ファイル（すべてコメントのみ）

- `collector/src/infrastructure/http/fetch-http-client.ts`
- `collector/src/infrastructure/http/sleep.ts`
- `collector/src/infrastructure/logging/console-logger.ts`
- `collector/src/domain/logger.ts`
- `collector/src/domain/datetime.ts`
- `collector/src/application/collect-articles.ts`

**BASE からの全差分でコメント行以外の増減が 0 件**であることを司令塔が実測確認した。テスト件数は着手前と同じ 533。

### 修正した指摘の要約

| 周 | 件数 | 主な内容 |
|---|---|---|
| 1-1 | — | spec・readability とも FAIL。(g) の対象が**タスクファイルの列挙より広かった**（下記）／`MAX_CAUSE_DEPTH` が実装に存在しない／`logger.ts` だけ「特記なき限り D-02」のヘッダが無く裸の `§4.7` が D-01 §4.7 と読める |
| 1-2 | 8 | 上記の修正。spec **PASS（指摘 0）**・readability PASS |
| 低の適用 | 4 | `LOG_URL_PREVIEW` の理由をタスクの作業範囲から恒久的な理由へ／`formatError` の設計書との差を明示／参照粒度の統一 |

### タスクファイルの誤り 2 件（司令塔が訂正）

1. **完了条件の grep 範囲が広すぎた。** 「`grep -rn "D-02 側の追随が必要" collector/src` が空」と書いたが、`main.ts`（`errorName`）と `firebase-notification-gateway.ts`（3 箇所）の計 4 件は **T-45 が残した未解決の設計ギャップ**で、D-02 側に規定が入るまで外せない（FOLLOWUPS 1-1・1-2・1-4・1-20）。**実装エージェントがこれを見つけて範囲外と判断したのが正しい**
2. **(g) の列挙を 3 件に絞ったのが誤り。** §8.1 (g) の原文は「…**等の**但し書きコメントを通常の参照コメントに直す」で、`fetch-http-client.ts` に 5 箇所残っていた。しかも **§5.7 手順 2b が「`finalUrl` は 200 文字に切り詰めてログに載せる」と明記しているのに、コメントは「任意値」と書いており事実と逆**だった（spec・readability が独立に指摘）

### 意図的に残したマーカー 2 件

いずれも「設計書に未反映」ではなく「**設計書との既知の差**」であることが読み取れる形にした。

- `fetch-http-client.ts`：設計書の表記は `AbortError` だが `AbortSignal.timeout` の実名は Node 22 の `TimeoutError`（実測差の記録）
- `logger.ts` の `formatError`：設計書のコード片は `MAX_CAUSE_DEPTH` のみで `seen` を持たない。実装は循環検知を追加している（FOLLOWUPS 1-21 への参照付き）

### 残っている「低」の指摘

なし。

### 対応しなかった指摘

いずれも設計書側の課題として `docs/tasks/FOLLOWUPS.md` に起票済み。

- §4.7 の `MAX_CAUSE_DEPTH` / §5.1 手順 6 の `message` 文言 / §4.2 の例外文言が実装と違う（**1-21**。挙動・分岐は一致しており文言だけの差）
- `RawArticleShapeSchema` を `domain/source.ts` へ寄せるかの検討（**4-11**）

### 開発者に確認してほしいこと

なし。
